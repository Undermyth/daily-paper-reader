---
title: "Erase-then-Delta Attention: Decoupling Erase and Write Addresses in Delta-Rule Linear Attention"
title_zh: 擦除-然后-增量注意力：在增量规则线性注意力中解耦擦除和写入地址
authors: "Xiao Li, Chengruidong Zhang, Hao Luo, Xi Lin, Zekun Wang, Zihan Qiu, Yunfei Mao, Langshi Chen, Man Yuan, Minmin Sun, Huiqiang Jiang, Siqi Zhang, Rui Men, Wei Hu, Gong Cheng, Bo Zheng, Dayiheng Liu, Jingren Zhou"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26560v1"
tags: ["query:la"]
score: 9.0
evidence: 提出擦除然后Delta注意力，一种新的线性注意力记忆更新规则，解耦擦除和写入地址，增强关联记忆能力。
tldr: 传统delta-rule线性注意力在更新记忆时只能纠正当前写入地址的内容，无法主动擦除其他地址的过时信息。为此提出Erase-then-Delta Attention，将擦除与写入解耦：先沿学习到的擦除方向选择性抑制过时记忆，再执行标准delta式修正写入。在2.5B密集模型和MoE 25B模型的语言模型预训练及长上下文微调中，EDA均取得最佳性能，长上下文评估范围4k-128k。分析表明，EDA保留delta-rule的修正能力，同时增加额外清理路径，尤其在被动衰减弱时效果显著，强调了循环记忆模型应同时决定写入和擦除。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26560v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1150, \"height\": 663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26560v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1625, \"height\": 424, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26560v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1095, \"height\": 201, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26560v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1175, \"height\": 493, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26560v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 910, \"height\": 204, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26560v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1405, \"height\": 144, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26560v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 992, \"height\": 393, \"label\": \"Table\"}]"
motivation: 传统delta-rule无法主动擦除其他地址的过时记忆，限制了记忆管理能力。
method: 提出Erase-then-Delta Attention，先学习擦除方向选择性擦除过时记忆，再执行delta式写入，解耦擦除与写入地址。
result: 在2.5B dense和MoE 25B模型预训练及长上下文微调中，EDA均取得最佳性能，长上下文评估范围4k-128k。
conclusion: EDA通过增加独立擦除路径增强了记忆管理，表明循环模型应同时决定写什么和擦除什么。
---

## 摘要
增量规则线性注意力通过先纠正当前写入地址中已存储的内容，再进行新内容的写入，从而改进了循环记忆更新。然而，主动纠正仍然局限于同一写入地址。因此，在不同地址存储的过时信息无法在其他位置写入新内容之前被主动移除。我们提出了擦除-然后-增量注意力（EDA），这是一种解耦擦除位置和写入位置的记忆更新规则。关键见解是，循环记忆模型不仅应纠正当前的写入，还应主动抑制独立选定地址上的过时记忆。具体来说，我们的方法首先沿学到的擦除方向执行有针对性的擦除步骤，然后沿当前写入方向执行标准的增量式纠正写入。这既保留了增量规则更新的纠正行为，又扩展了其记忆管理能力。语言模型预训练实验在密集2.5B和MoE 25B-A2.8B模型族上进行，结果显示EDA在两种设置下均表现最佳。在MoE模型经过800亿token的长上下文中期训练后，这一优势依然存在，且在4k到128k上下文的长上下文评估中EDA也表现最佳。紧凑更新分析和记忆状态探测揭示了原因：EDA保持增量规则纠正写入不变，同时在被动衰减较弱时最强烈地分配额外的清理路径。这些结果表明，循环记忆模型不仅应决定写入什么，还应决定擦除哪些过时信息以及在哪里擦除。

## Abstract
Delta-rule linear attention improves recurrent memory updates by correcting what is already stored at the current write address before writing new content. However, the active correction is still anchored to that same write address. As a result, stale information stored at a different address cannot be actively removed before new content is written elsewhere. We propose Erase-then-Delta Attention (EDA), a memory update rule that decouples where to erase from where to write. The key insight is that recurrent memory models should not only correct the current write, but also selectively suppress outdated memory at an independently chosen address. Concretely, our method first applies a targeted erase step along a learned erase direction, and then performs the standard delta-style corrective write along the current write direction. This preserves the corrective behavior of delta-rule updates while expanding their memory-management capacity. Language-model pretraining experiments across dense 2.5B and MoE 25B-A2.8B model families show that EDA performs best in both settings. The gain persists after 80B-token long-context midtraining of the MoE models, where EDA also performs best in long-context evaluations from 4k to 128k contexts. A compact update analysis and memory-state probes suggest why: EDA keeps the delta-rule corrective write intact while allocating an additional cleanup path most strongly when passive decay is weak. These results suggest that recurrent memory models should decide not only what to write, but also what stale information to erase and where.

---

## 论文详细总结（自动生成）

# Erase-then-Delta Attention: Decoupling Erase and Write Addresses in Delta-Rule Linear Attention — 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：Delta-rule线性注意力通过先纠正当前写入地址中已存储的内容，再进行新内容写入，改善了循环记忆更新。然而，主动纠正仍然局限于**同一个写入地址**。这导致存储在其他地址的过时信息无法在新内容写入之前被主动移除，只能依靠对角衰减（channel-wise forgetting）这种与地址无关的被动机制来消退。
- **核心动机**：循环记忆模型不仅应决定**写入什么**，还应决定**在哪里擦除过时信息**。现有方法（如Gated DeltaNet、KDA等）的擦除和写入在地址层面是耦合的（都基于当前写入键 \(k_t\)），缺乏能力在独立选择的地址上执行目标性擦除。
- **整体含义**：提出Erase-then-Delta Attention（EDA），在delta-rule更新中增加一个独立的擦除步骤，先沿学到的擦除方向抑制过时记忆，再执行标准delta式纠正写入，从而解耦“擦除地址”和“写入地址”，扩展记忆管理容量。

## 2. 方法论：核心思想、关键技术细节、公式（文字说明）

- **核心思想**：在标准的对角线门控delta更新（DPLR风格）中插入一个**独立寻址的擦除操作**，与后续的delta写入顺序执行。擦除地址 \(e_t\) 是可学习的，与写入键 \(k_t\) 解耦。
- **更新规则**：
  \[
  S_t = (I - \beta_t k_t k_t^\top)(I - \gamma_t e_t e_t^\top) D_t S_{t-1} + \beta_t k_t v_t^\top
  \]
  其中：
  - \(D_t = \text{Diag}(\alpha_t)\)：对角衰减门（每通道保留率）
  - \((I - \gamma_t e_t e_t^\top)\)：擦除项，沿 \(e_t\) 方向压缩衰减后的记忆，目标是使该地址的读出响应接近零
  - \((I - \beta_t k_t k_t^\top)\)：标准delta修正项，在写入键方向进行纠正
  - 最后一项 \(\beta_t k_t v_t^\top\)：写入新值

- **关键设计细节**：
  - **擦除目标**：擦除操作通过梯度下降最小化 \(\frac{1}{2} \| \tilde{S}_t^\top e_t \|^2\)，即主动抑制在 \(e_t\) 查询时的响应。
  - **安全门控**：对数空间衰减使用有界参数化 \(g_t = \ell + (-\ell) \exp(-A/|\ell| \odot \Delta_t)\)，保证每个衰减因子 \(\alpha_{t,i} \in (\exp(\ell), 1]\)，防止数值下溢，兼容半精度。
  - **更新顺序**：擦除先于delta写入，确保擦除只作用于旧内容，不会影响刚写入的新信息。
  - **低秩实现**：擦除地址 \(e_t\) 通过低秩投影生成，节省参数。
  - **分块并行**：通过将单个时间步拆分为两个子步（擦除子步和delta子步），EDA可转化为标准DPLR循环，利用现有分块并行内核高效计算。

- **算法流程**（文字描述）：
  1. 对每个 token \(t\)，投影生成 query、key、value、delta gate \(\beta_t\)、擦除 gate \(\gamma_t\)、衰减参数 \(\alpha_t\)、擦除地址 \(e_t\)。
  2. 应用对角线衰减 \(D_t\) 到上一状态 \(S_{t-1}\)。
  3. 沿擦除方向 \(e_t\) 以强度 \(\gamma_t\) 压缩衰减后记忆（擦除步骤）。
  4. 再沿写入键 \(k_t\) 以强度 \(\beta_t\) 执行delta修正（纠正旧内容并写入新值）。
  5. 新状态 \(S_t\) 用于下一步，同时输出由 query 读取的值。

## 3. 实验设计：数据集、Benchmark、对比方法

- **预训练阶段**：
  - 训练数据：未指定具体语料，但使用400B tokens，序列长度4096，全局batch size 1024。
  - 对比方法：Transformer（全注意力）、GDN、GDN-2、KDA、EDA（均在相同的混合架构下，3:1线性-全注意力比例）。
- **评估Benchmark**：
  - 知识/推理：MMLU、MMLU-Pro、GSM8K、MATH、BBH、EvalPlus（代码生成）。报告平均分（Avg.）。
- **长上下文评估**：
  - RULER任务，上下文长度从4k到128k（其中64k、128k是外推），MoE模型经过80B tokens的midtraining（序列长度32k）后评估。
- **内存状态分析**：
  - 分析EDA的擦除地址、衰减门、delta门的分配比例；通过替换擦除方向（随机、打乱、设为与键相同）对比读取扰动；测量擦除方向与写入键的余弦相似度。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长等信息，仅描述了训练规模（400B tokens，batch size 1024，序列长度4096）及midtraining规模（80B tokens，序列长度32k）。未提及具体硬件配置及训练时间。因此无法给出确切的资源与算力细节。

## 5. 实验数量与充分性

- **实验数量**：
  - 预训练：Dense 2.5B + MoE 25B-A2.8B两种规模，每种包含多个对比模型（基线+变体），共约5-6个模型。
  - Midtraining：仅MoE模型，三个对比（GDN、KDA、EDA）。
  - 长上下文评估：RULER在6个长度点、4次运行取平均。
  - 内存状态分析：含门控分配、扰动比较、余弦相似度等多项探测。
- **充分性判断**：
  - 实验覆盖了不同规模（密集、MoE）、不同训练阶段（预训练、midtraining）、不同任务类型（知识、推理、代码、长上下文），范围较广。
  - 对比方法包括主流基线（GDN、GDN-2、KDA），且共享相同混合架构和训练配置，保证了公平性。
  - 内存状态分析提供了机制层面的解释，增强了结论可信度。
  - **结论**：实验设计比较充分，结果客观可信。

## 6. 主要结论与发现

- **性能提升**：在Dense 2.5B和MoE 25B两种规模下，EDA平均分均最高；在MoE预训练后经80B tokens长上下文中期训练，EDA依然保持最佳成绩（尤其MMLU、MATH、BBH等）。
- **长上下文能力**：RULER评估中，EDA在短上下文（4k-16k）表现最好，在32k-128k接近或超过基线，平均分最高。
- **内存机制**：
  - 擦除路径在被动衰减弱（\(\bar{\alpha}\) 接近1）的头部贡献突出（占69%），说明EDA在需要持久记忆时提供额外清理能力。
  - 学到的擦除方向比随机、打乱、等于键的方向引起的读取扰动更小，表明擦除更具选择性。
  - 擦除方向与写入键的平均余弦绝对值约0.105，接近独立随机方向，远小于同地址情况（1.0），验证了地址解耦。
- **局限性**：EDA的raw write-key recall（原始键的回忆能力）低于KDA，表明擦除路径并非全面提升记忆保真度，而是条件性清理机制。

## 7. 优点

- **方法简洁直观**：仅增加一个低秩擦除方向和一个标量门，即实现地址解耦，不改变delta-rule核心结构。
- **实验充分且公平**：在两种参数量级、多种任务上对比多个强基线，共享架构和训练配置。
- **机制分析深入**：通过门控分配、扰动对比、余弦相似度等探测，从多个角度验证了擦除路径的独立性和作用模式。
- **工程实现友好**：提供分块并行优化方案（WY表示、UT变换），可将EDA映射到现有DPLR内核，减少实际开销。
- **性能持续领先**：在预训练和长上下文中均保持优势，表明方法具备鲁棒性和可扩展性。

## 8. 不足与局限

- **原始键回忆能力下降**：EDA的raw write-key recall低于KDA，说明擦除操作可能同时削弱了部分有用信息的可追溯性，并非无条件优于其他方法。
- **机制间接证据**：当前探测（门分配、扰动）未直接追踪单个擦除事件如何对应到具体下游预测改善，缺乏因果链验证。
- **未探索更大规模**：实验只到25B参数（MoE有效参数2.8B），更大规模（如70B+）下的表现未知。
- **未涵盖所有可能基线**：如DeltaProduct、RWKV-7、Comba等更丰富的状态转移方法未被直接对比（论文指出这是正交方向）。
- **资源/效率分析缺失**：虽然提及计算开销可控，但未提供实际训练/推理的GPU小时数、吞吐量对比等量化数据。

（完）
