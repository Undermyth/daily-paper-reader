---
title: "ATMA: Length-Invariant Language Modeling via Polar Attention and Gated-Delta Compression Memory"
title_zh: "ATMA: 通过极化注意力与门控增量压缩记忆实现长度不变的语言建模"
authors: Habibullah Akbar
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25156v1"
tags: ["query:la"]
score: 9.0
evidence: 结合极性注意力和门控Delta压缩记忆的混合架构，用于长上下文建模
tldr: "现代大语言模型基于softmax注意力，在长上下文下因概率质量稀释导致性能崩溃。ATMA提出混合卷积-注意力架构，通过极坐标注意力分解为方向通道和幅度通道，并配合门控delta压缩记忆实现长度不变性。在64K token长度下保持90%以上检索准确率，困惑度单调降低，远超softmax基线。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25156v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1226, \"height\": 831, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25156v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1434, \"height\": 573, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25156v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1385, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1245, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1307, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1346, \"height\": 209, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1440, \"height\": 650, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1451, \"height\": 1711, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1993, \"height\": 771, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1992, \"height\": 1285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1992, \"height\": 1284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1992, \"height\": 1016, \"label\": \"Table\"}]"
motivation: 解决softmax注意力随序列增长导致长上下文性能崩溃，以及滑动窗口与全上下文注意力的结构性矛盾。
method: 引入极坐标注意力，分解为方向通道和幅度通道（基于有效匹配参与比与极端值校正空汇），结合门控delta快权重规则的循环压缩记忆。
result: "在100次消融实验中，组合模型在64K token（32倍训练长度）上检索准确率>90%，文档困惑度单调改善。"
conclusion: 极坐标注意力与循环记忆协同实现长度不变的语言建模，显著提升长上下文能力。
---

## 摘要
现代基于softmax缩放点积注意力的大语言模型受限于其训练序列长度：随着键值序列增长，softmax概率质量会扩散到更宽的分布中，导致激活偏移和长上下文性能崩溃。此外，长上下文语言建模面临结构性矛盾：滑动窗口注意力核心保持有界局部表示和低困惑度，但无法捕捉长距离依赖；而全上下文注意力保留全局召回能力，但遭受分布外困惑度爆炸。为解决这些限制，我们提出ATMA，一种混合卷积-注意力架构，整合了新颖的三通道注意力机制。ATMA将注意力混合步骤分解为：(1) 计数无关的单位向量方向通道，(2) 由有效匹配参与率经极端值校正零汇驱动的有界幅度通道，以及(3) 通过门控增量快速权重规则优化的长期循环压缩记忆。极化注意力核心与循环记忆单独均不足够；它们的组合同时实现了单调的困惑度降低和高保真度长距离检索。我们通过100次运行的因子消融扫描评估ATMA，证明组合的极化+记忆模型在64K token（训练长度2K的32倍）外维持感应针寻草检索准确率>90%，同时其文档困惑度单调改善，优于在极端上下文长度下崩溃的基于softmax的记忆基线。代码：https://github.com/kreasof-ai/atma

## Abstract
Modern large language models based on softmax scaled-dot-product attention are constrained by their training sequence length: as the key-value sequence grows, softmax probability mass can dilute across a wider distribution, inducing activation shift and long-context performance collapse. Moreover, long-context language modeling faces a structural tension: a sliding-window attention core maintains a bounded local representation and low perplexity but is blind to long-range dependencies, while full-context attention preserves global recall but suffers from out-of-distribution perplexity explosion. To resolve these limitations, we introduce ATMA, a hybrid convolutional-attention architecture that integrates a novel three-channel attention mechanism. ATMA factorizes the attention mixing step into: (1) a count-blind, unit-vector direction channel, (2) a bounded magnitude channel driven by the participation ratio of effective matches over an extreme-value-corrected null sink, and (3) a long-term recurrent compression memory optimized via a gated-delta fast-weights rule. Neither the Polar Attention core nor the recurrent memory is sufficient alone; their combination enables monotonic perplexity reduction and high-fidelity long-range retrieval simultaneously. We evaluate ATMA using a 100-run factorial ablation sweep, demonstrating that the combined Polar + memory model maintains induction needle-in-a-haystack retrieval accuracy above 90% out to 64K tokens (32 times the training length of 2K) while its document perplexity improves monotonically, outperforming softmax-based memory baselines which collapse at extreme context lengths. Code: https://github.com/kreasof-ai/atma

---

## 论文详细总结（自动生成）

# ATMA论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：标准 softmax 缩放点积注意力在超出训练序列长度时性能严重下降，表现为概率质量稀释（dilution）导致的激活偏移和长上下文性能崩溃。同时，长上下文建模存在结构性矛盾：滑动窗口注意力保持低困惑度但无法获取长距离依赖，全上下文注意力保留全局召回但遭遇分布外困惑度爆炸。
- **整体含义**：本文旨在设计一种**长度不变（length-invariant）的序列混合器**，同时实现高效的局部建模和可靠的远距离精确检索，避免传统的 trade-off。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **架构总览**：16 层解码器，采用 3:1 混合比例 —— 12 层 LFM2 门控卷积层 + 4 层 Polar Attention 层。每层使用预归一化布局。
- **Polar Attention（极坐标注意力）**：将注意力输出分解为三个通道：
    1. **方向通道（content）**：将值向量投影到单位球面，获得“什么特征被匹配”，完全忽略计数。
    2. **幅度通道（count）**：利用参与比（effective participation ratio，即逆 Simpson 指数）衡量“有多少匹配”，通过饱和 tanh 映射到 [0,1) 区间，防止激活偏移。
    3. **记忆通道（memory）**：由门控增量（gated-delta）更新的循环关联记忆（Titans 式记忆）。
    - 关键改进：引入了每个头的**长度温度**（length temperature）和**极端值校正零汇**（extreme-value-corrected null sink），防止 softmax 稀释和噪声干扰。
    - **长度温度**：temp = 1 + softplus(α)·log(n)，随序列长度 n 增长而增加，使注意力分布更尖锐。
    - **零汇**：null = null_base + softplus(γ)·√log(n+1)，模拟随机噪声的最大值增长，提供一个可学习的“空 sink”键。
    - **对齐损失**（distractor loss）：训练时迫使随机键得分低于零汇，但在最终配置中未使用（因为记忆分支激活后其会过度锐化）。
- **Gated-Delta 记忆（Titans Memory-as-Gate）**：
    - 每个头维护一个矩阵状态 M ∈ R^{dv×dk}，作为快速权重关联存储。
    - 更新规则：M_t = γ_t·M_{t-1}(I - β_t k_t k_t^⊤) + β_t v_t k_t^⊤
    - **发现 1：键归一化**：必须对键和查询做 L2 归一化（||k||=1），否则状态范数会指数爆炸。
    - **发现 2：自稳定**：由于 (I - βkk^⊤) 连续投影出旧匹配维度，状态范数在长度扫描中保持平稳。
- **最终输出**：out = out_polar + out_mem，记忆读出的向量经过 RMS 归一化和门控后加入残差流。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
    - 训练：FineWeb-Edu 语料库，训练 token 数 10 亿（1 epoch），序列长度 2048。
    - 评估：
        - 文档困惑度（perplexity）：FinePDFs 语料中的连贯长文档。
        - 检索任务：**Induction Needle-in-a-Haystack**（针寻草）：在长文本“草堆”中植入形如“The access code for record [KEY] is [D1][D2]…[D5]”的诱导“针”，在远端重复提示键，测评 5 位数字的逐位准确率（距离最长 64K tokens）。
- **对比方法**：
    - 注意力类型：Polar（本文）、softmax with RoPE（rope）、softmax without positional encoding（nope）。
    - 记忆分支：on/off。
    - 滑动窗口：on（窗口 1024）/off。
    - 正则化模式（weak、strong、discrete、zipfian）和干扰损失 on/off。
- **主要基线**：Softmax + Titans Memory，Softmax without memory，Polar without memory。

## 4. 资源与算力

- **GPU 型号**：NVIDIA L4（24GB）。
- **训练配置**：
    - 模型参数量：369.72M（非嵌入参数）。
    - 优化器：Muon（序列混合器和 MLP）+ AdamW（嵌入和归一化层）。
    - 学习率：余弦退火调度，70% 冷却比例。
    - 训练时长：未明确说明总训练小时数，但给出了每步时间（Polar-only：28.33s/step；Polar+Memory：32.49s/step，MFU 分别为 40.1% 和 35.6%）。100 个配置各训练 1B tokens，需根据步数推算（序列长度 2048，总 step ≈ 1B/2048 ≈ 488,281 步，单配置约 28s×488k ≈ 162 天？但实际应为多 GPU 并行，论文未明确 GPU 数量）。可总结为：使用 L4 GPU，训练了 100 个 370M 参数模型，每个模型训练 1B tokens。

## 5. 实验数量与充分性

- **实验数量**：共 **100 次运行**的因子消融扫描（factorial ablation sweep），覆盖 5 个轴：注意力类型（3 种）、正则模式（5 种）、干扰损失（2 种）、记忆分支（2 种）、滑动窗口（2 种），不完全完全组合但覆盖主要空间。
- **充分性**：
    - 消融覆盖了注意力核心设计、记忆、窗口、辅助损失各组件，对比了 3 种注意力基线（softmax+RoPE、无位置编码 softmax、Polar）。
    - 评估指标包括文档困惑度、检索准确率、训练 MFU，并展示了随上下文长度从 2K 到 64K 的完整趋势。
    - 检索任务使用诱导检索（needle-in-a-haystack），具有明确的难度控制。
    - 优势：实验设计严格正交，表格完整（附录 C 给出全部 100 个配置的详细指标），结论由数据支撑。
    - 潜在不足：训练数据仅 1B tokens（单 epoch），未验证在大规模预训练（如 100B+ tokens）下的表现；模型规模仅 370M，未验证扩展到更大模型时的表现。

## 6. 论文的主要结论与发现

- **注意力单独无法泛化**：无论 softmax 还是 Polar，不加记忆和窗口，在 8K 以上检索准确率急剧下降，4×训练长度后接近零。
- **Polar + Titans 记忆组合实现最优**：
    - 检索准确率在 64K (32×训练长度) 上保持 >90%（平均约 94%）。
    - 文档困惑度从 2.70→1.96（单调下降），优于 softmax+记忆基线（2.66→2.34），且困惑度改善说明模型积极利用了更长上下文。
    - 软max+记忆在极端长度（64K）检索准确率仅 16%，大幅落后。
- **窗口和干扰损失在记忆激活时有害**：窗口限制注意力视野，干扰损失过度锐化零汇，均损害检索。
- **门控增量记忆自带稳定性**：键归一化后状态范数保持平坦，无发散。

## 7. 优点：方法或实验设计上的亮点

- **理论清晰的问题分解**：将注意力解耦为方向（what）和幅度（how many），从根本上避免 softmax 的计数-方向纠缠问题。
- **有效的长度温度 + 极端值零汇**：利用数学直觉（随机噪声最大值的渐近行为）设计可学习参数，使注意力分布自动适应序列长度。
- **混合架构高效**：仅在 4/16 层使用 Polar Attention，其余用卷积，大幅降低计算开销；Triton 实现的自定义核使记忆分支的 MFU 下降仅 4.5 个百分点。
- **消融实验详尽且透明**：100 个配置，每种配置给出训练验证损失、2K/64K 困惑度、检索准确率、MFU，并附完整表格，结论可复现。
- **硬件协同设计**：针对 Polar Attention 的 FlashAttention 风格 Triton 核（在线参与比计算）、GQA 分组分页推理核、原地门控增量步骤核，提升效率。

## 8. 不足与局限

- **模型规模较小**（370M），未在 1B+ 或更大模型上验证，泛化到更大模型的不确定性。
- **训练数据量有限**：仅 1B tokens（单 epoch），未展示随训练数据增加性能如何变化。
- **检索任务单一**：仅使用 inductive needle-in-a-haystack，未测试更复杂的多跳推理、文档 QA 等任务。
- **未与最先进的长上下文模型（如 Mamba2、Gated DeltaNet 等）直接比较**：仅对比了 softmax 注意力和自身消融配置，缺乏与同参数规模的线性注意力 / SSM 模型在相同设置下的公平对比（虽然提到了部分基线，但未复现其他 SOTA 模型）。
- **训练优化器特殊**：使用 Muon 优化器，并非主流 AdamW，可能引入偏差；且仅使用了 70% 余弦退火冷却，未说明最优调度是否改变。
- **消融中 distractor loss 最终未使用**，但训练时有的配置使用了该损失，这些配置的 MFU 下降更大（20.7%），该损失的设计本身可能存在过度锐化问题。
- **工程实现细节多**：依赖自定义 Triton 核、Flash Linear Attention 库等，复现门槛较高。

（完）
