---
title: Adaptive Hebbian Memory Routing in Vision Transformers for Few-Shot Learning
title_zh: 面向小样本学习的视觉Transformer中的自适应赫布记忆路由
authors: "Mohammed Yusuf Mujawar, Noorbakhsh Amiri Golilarz"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24756v1"
tags: ["query:la"]
score: 9.0
evidence: 视觉Transformer中的自适应Hebbian记忆路由
tldr: "少样本图像识别面临固定Hebbian记忆适应性不足的问题。本文提出自适应Hebbian路由，通过轻量MLP路由器动态控制记忆贡献、更新强度和保留率。在多个Vision Transformer骨干上，完全自适应路由在Omniglot达到96.94%准确率，推理时间降低15%。该方法在CIFAR-FS等数据集上普遍提升性能，证明自适应机制能超越固定Hebbian行为。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24756v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 1400, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24756v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1710, \"height\": 680, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24756v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 933, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24756v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 936, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24756v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 935, \"height\": 204, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24756v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 935, \"height\": 685, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24756v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 933, \"height\": 685, \"label\": \"Table\"}]"
motivation: 固定Hebbian记忆行为无法适应不同少样本任务，需要自适应控制记忆更新和作用。
method: 提出自适应Hebbian路由，利用MLP路由器调节记忆贡献、更新强度和保留率，实现全自适应。
result: "Swin-Tiny完全自适应路由在Omniglot达96.94%，推理时间从16.51ms降至14.05ms；CIFAR-FS上多种骨干性能提升。"
conclusion: 自适应机制能超越固定Hebbian行为，提升少样本Transformer表示能力。
---

## 摘要
小样本图像识别要求模型能够从少量标注的支持集中适应新类别。赫布快速权重记忆可以在一个情节中提供临时关联信息，但固定的记忆行为可能并不适用于每个小样本任务。在这项工作中，我们提出了用于小样本视觉Transformer的自适应赫布路由。该方法使用轻量级MLP路由器来控制赫布记忆的贡献、记忆更新的强度以及从支持集特征中保留先前记忆的程度。我们研究了自适应放置、自适应可塑性和完全自适应赫布路由。实验使用ViT-Small、DeiT-Small和Swin-Tiny，在Omniglot、CIFAR-FS以及从CIFAR-FS到Omniglot的跨域迁移上进行5-way 1-shot评估。在直接的Swin比较中，固定和自适应赫布变体使用相同的记忆位置。自适应可塑性将固定赫布的结果从96.74%提高到96.92%，而完全自适应路由以96.94%达到最佳结果。与固定赫布Swin相比，完全自适应Swin模型还将推理时间从16.51毫秒减少到14.05毫秒。在CIFAR-FS上，自适应变体在所有三个骨干网络上均提升了性能，并且多镜头评估表明，随着支持示例数量的增加，这些增益仍然有效。这些结果表明，自适应可塑性和自适应记忆激活可以超越固定赫布行为，改进小样本Transformer表示。

## Abstract
Few-shot image recognition requires models to adapt to new classes from a small labeled support set. Hebbian fast-weight memory can provide temporary associative information during an episode, but fixed memory behavior may not be appropriate for every few-shot task. In this work, we propose Adaptive Hebbian Routing for few-shot Vision Transformers. The method uses a lightweight MLP router to control the contribution of Hebbian memory, the strength of memory updates, and the retention of previous memory from support-set features. We study Adaptive Placement, Adaptive Plasticity, and Fully Adaptive Hebbian Routing. Experiments use ViT-Small, DeiT-Small, and Swin-Tiny under 5-way 1-shot evaluation on Omniglot, CIFAR-FS, and cross-domain transfer from CIFAR-FS to Omniglot. In the direct Swin comparison, fixed and adaptive Hebbian variants use the same memory location. Adaptive Plasticity improves the fixed Hebbian result from 96.74\% to 96.92\%, while Fully Adaptive Routing achieves the best result at 96.94\%. The fully adaptive Swin model also reduces inference time from 16.51 ms to 14.05 ms relative to fixed Hebbian Swin. On CIFAR-FS, adaptive variants improve performance across all three backbones, and the multi-shot evaluation shows that these gains remain useful as the number of support examples increases. These results show that adaptive plasticity and adaptive memory activation can improve few-shot Transformer representations beyond fixed Hebbian behavior.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
小样本图像识别要求模型从极少量标注支持集中快速适应新类别。赫布快速权重记忆可在单次情节中存储临时关联信息，但现有方法**对所有任务使用相同的固定记忆行为**（如固定的激活强度、更新率和衰减率），这往往不适应不同任务的语义变化。研究表明，固定赫布记忆的效果高度依赖插入位置，且在扁平Transformer（ViT、DeiT）上可能降低性能。因此，核心问题是如何让赫布记忆**根据当前支持集动态调整**，从而提升小样本适应能力。

### 2. 论文提出的方法论：核心思想、关键技术细节
**核心思想**：引入轻量级**MLP路由器**，根据当前情节的支持集特征预测控制参数，实现记忆贡献、写入强度和保留率的自适应调节。

**关键技术细节**：
- **赫布快速权重模块**：输入token表示\(X\)投影为K、V、Q，计算关联矩阵\(A = \text{clip}(K^\top V / \sqrt{T}, -\delta, \delta)\)，记忆更新为\(M \leftarrow \lambda M + \eta A\)，其中\(\eta\)为可塑性率，\(\lambda\)为衰减因子。记忆被Frobenius范数归一化，每情节重置。
- **自适应MLP路由器**：在选定的骨干位置\(l\)，对支持集特征做均值池化得到情节级向量\(z_l\)，通过两层MLP（隐藏维度为输入维度的1/8，最小16）输出控制值：
  - 外部门控\(g_l = \sigma(r_g)\)控制记忆通路贡献；
  - 可塑性率\(\eta_l = \eta_{\max} \sigma(r_\eta)\)（\(\eta_{\max}=0.15\)）；
  - 衰减因子\(\lambda_l = \sigma(r_\lambda)\)。
- **三种自适应变体**：
  - Adaptive Placement：仅学习\(g_l\)，固定\(\eta, \lambda\)；
  - Adaptive Plasticity：学习\(\eta_l, \lambda_l\)，固定门控为开；
  - Fully Adaptive Hebbian Routing：同时学习\(g_l, \eta_l, \lambda_l\)。
- **集成方式**：支持集先写入共享记忆，支撑和查询特征均从该记忆读取后再继续前向传播。路由器参数通过梯度下降联合优化。

### 3. 实验设计：数据集、基准与对比方法
- **数据集**：
  - **Omniglot**：1623类字符，训练/测试按标准划分，从零训练。
  - **CIFAR-FS**：100类60k图像，训练/测试64/36类，使用ImageNet预训练骨干微调。
  - **跨域迁移**：CIFAR-FS上训练 → Omniglot测试（无目标域微调）。
- **评估协议**：5-way 1-shot（及3、5、10-shot多shot分析），原型网络分类，报告平均情节准确率。
- **基准模型**：
  - Base（无赫布记忆）；
  - Fixed Hebbian（固定门控、固定\(\eta,\lambda\)，在相同或不同位置插入）；
  - 提出的三种自适应变体。
- **骨干网络**：ViT-Small、DeiT-Small、Swin-Tiny（均为参数约20-30M的小型变体）。

### 4. 资源与算力
论文明确提到：“All experiments are implemented in PyTorch and executed on an **NVIDIA RTX 5090 GPU**。” 但**未说明具体GPU数量、训练时长或总实验耗时**。因此，无法量化训练资源消耗。

### 5. 实验数量与充分性
**实验组数**：
- 3个数据集 × 3个骨干 × 4种方法（Base/Fixed/Adaptive Placement/Adaptive Plasticity/Fully Adaptive），共约9×5=45个主结果（但部分组合因设计原因未报告，如Swin直接比较时仅用同一位置）。
- 额外多shot分析（1、3、5、10-shot）在CIFAR-FS上绘制曲线。
- 参数和推理时间对比表（表IV、V）。
- 消融实验：三种变体分离了门控、可塑性/衰减的作用。

**充分性评价**：
- **优点**：覆盖多个骨干、数据集和域迁移场景；直接比较（Swin相同记忆位置）隔离了放置效应；多shot验证泛化性；效率指标完整。
- **不足**：未与传统元学习方法（如MAML、ProtoNet+ResNet）或最新的小样本Transformer（如FewTURE）对比；未在更大规模数据集（如miniImageNet）上评估；跨域结果存在骨干依赖性，结论的普遍性有限。

### 6. 论文的主要结论与发现
1. **自适应可塑性是关键**：在Swin直接比较中，Adaptive Placement（仅学习门控）未超过固定赫布，而Adaptive Plasticity提高了0.18个百分点，Fully Adaptive最高达96.94%。
2. **自适应路由在扁平Transformer上恢复性能**：对ViT和DeiT，固定密集赫布插入大幅降低Omniglot准确率（95.42%→89.99%），而自适应路由恢复至95.48%，接近Base。
3. **CIFAR-FS上所有自适应变体均大幅优于固定赫布**：ViT提升16个百分点，Swin提升7.3个百分点。
4. **推理时间节省**：完全自适应Swin推理时间从16.51ms降至14.05ms；ViT/DeiT因减少赫布模块数量，推理时间缩短近一半。
5. **多shot下收益保持**：自适应变体在3/5/10-shot上仍优于固定赫布基线。
6. **跨域迁移结果骨干依赖**：Swin的完全自适应和DeiT的Adaptive Placement改善，但ViT上固定赫布最优。

### 7. 优点：方法或实验设计亮点
- **轻量高效**：MLP路由器仅增加少量参数（Swin上+0.07M），且通过减少赫布模块数量实现推理加速。
- **结构清晰**：三种变体逐步消融，分离了记忆贡献与记忆更新策略的效果。
- **控制变量设计**：Swin部分固定插入位置，直接对比自适应 vs 固定行为，排除了放置影响。
- **多维度评估**：除了准确率，还报告参数量和每情节推理时间，体现实际部署价值。
- **跨域验证**：检测了方法的泛化鲁棒性，暴露了局限性（如ViT表现不一致）。

### 8. 不足与局限
- **与SOTA对比缺失**：未与MAML、ProtoNet+ResNet或更复杂的元学习Transformer（如FEAT、CTX）进行公平比较，无法直接判断整体绝对水平。
- **数据集局限**：Omniglot和CIFAR-FS图像分辨率低（32×32级别），未在更高分辨率的miniImageNet或tieredImageNet上验证。
- **跨域结果不一致**：ViT上自适应未超过固定赫布，说明算法对骨干架构的敏感性。
- **记忆写入方式简化**：支持集所有样本共享一个记忆矩阵，未探索逐样本或多头独立记忆。
- **未讨论超参数敏感度**：如\(\eta_{\max}=0.15\)的选取依据未提供消融。
- **可重复性细节不足**：未提供学习率、训练步数、数据增强等完整超参数。

（完）
