---
title: "ATMA: Length-Invariant Language Modeling via Polar Attention and Gated-Delta Compression Memory"
title_zh: ATMA：通过极化注意力与门控-德尔塔压缩记忆实现长度不变的语言建模
authors: Habibullah Akbar
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25156v2"
tags: ["query:la"]
score: 9.0
evidence: 混合架构，使用极性注意力和门控-德尔塔压缩记忆实现长度不变性
tldr: "现代大语言模型基于softmax注意力受限于训练序列长度，长上下文时概率稀释导致性能崩溃。ATMA提出混合卷积-注意力架构，通过三通道极坐标注意力分解方向、幅度和长时记忆，结合门控delta快速权重压缩记忆。实验表明，ATMA在64K tokens（32倍训练长度）上保持90%以上检索准确率，困惑度单调改善，优于softmax基线。该工作实现了长度不变的语言建模，兼具局部高效与全局记忆。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25156v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1226, \"height\": 831, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25156v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1434, \"height\": 573, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25156v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1028, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1357, \"height\": 371, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1308, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1444, \"height\": 666, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1108, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1353, \"height\": 209, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1438, \"height\": 649, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1443, \"height\": 1711, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1994, \"height\": 769, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1992, \"height\": 1285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1992, \"height\": 1285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-012.webp\", \"caption\": \"\", \"page\": 0, \"index\": 12, \"width\": 1992, \"height\": 1284, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25156v2/table-013.webp\", \"caption\": \"\", \"page\": 0, \"index\": 13, \"width\": 1990, \"height\": 619, \"label\": \"Table\"}]"
motivation: 解决softmax注意力在长上下文中的概率稀释和性能崩溃，以及滑动窗口与全上下文之间局部与全局的权衡。
method: 引入三通道极坐标注意力：方向通道（count-blind）、幅度通道（参与比校正）、长时记忆通道（门控delta压缩），并混合卷积。
result: "在2K训练长度上，64K上下文检索准确率>90%，困惑度单调提升，优于softmax记忆基线。"
conclusion: 极坐标注意力与递归记忆组合实现长度不变性，同时保持低困惑度和高召回。
---

## 摘要
现代基于softmax缩放点积注意力的大语言模型受限于其训练序列长度：随着键-值序列增长，softmax概率质量会在更宽的分布上稀释，导致激活偏移和长上下文性能崩溃。此外，长上下文语言建模面临结构性张力：滑动窗口注意力核心维持有界的局部表示和低困惑度，但忽视长程依赖；而全上下文注意力保持全局召回，却遭受分布外困惑度爆炸。为解决这些限制，我们引入ATMA，一种混合卷积-注意力架构，集成了新颖的三通道注意力机制。ATMA将注意力混合步骤分解为：(1) 对计数不敏感的单位向量方向通道，(2) 由有效匹配参与率驱动的有界幅度通道，该参与率经极值校正空汇修正，以及(3) 通过门控-德尔塔快速权重规则优化的长期循环压缩记忆。极化注意力核心与循环记忆单独均不足；它们的组合能够同时实现单调的困惑度降低和高保真长程检索。我们使用120次运行的全因子消融扫描评估ATMA，结果表明，组合的极化+记忆模型在64K token（训练长度2K的32倍）下仍保持诱导的“大海捞针”检索准确率超过90%，同时其文档困惑度单调改善，优于在极端上下文长度下崩溃的基于softmax的记忆基线。代码：https://github.com/kreasof-ai/atma

## Abstract
Modern large language models based on softmax scaled-dot-product attention are constrained by their training sequence length: as the key-value sequence grows, softmax probability mass can dilute across a wider distribution, inducing activation shift and long-context performance collapse. Moreover, long-context language modeling faces a structural tension: a sliding-window attention core maintains a bounded local representation and low perplexity but is blind to long-range dependencies, while full-context attention preserves global recall but suffers from out-of-distribution perplexity explosion. To resolve these limitations, we introduce ATMA, a hybrid convolutional-attention architecture that integrates a novel three-channel attention mechanism. ATMA factorizes the attention mixing step into: (1) a count-blind, unit-vector direction channel, (2) a bounded magnitude channel driven by the participation ratio of effective matches over an extreme-value-corrected null sink, and (3) a long-term recurrent compression memory optimized via a gated-delta fast-weights rule. Neither the Polar Attention core nor the recurrent memory is sufficient alone; their combination enables monotonic perplexity reduction and high-fidelity long-range retrieval simultaneously. We evaluate ATMA using a 120-run factorial ablation sweep, demonstrating that the combined Polar + memory model maintains induction needle-in-a-haystack retrieval accuracy above 90% out to 64K tokens (32 times the training length of 2K) while its document perplexity improves monotonically, outperforming softmax-based memory baselines which collapse at extreme context lengths. Code: https://github.com/kreasof-ai/atma

---

## 论文详细总结（自动生成）

### 论文中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：现代大语言模型（LLM）广泛使用的 **softmax 缩放点积注意力** 在长上下文场景中存在严重限制。随着键-值序列长度增长，softmax 概率质量被稀释（**dilution**），导致“激活漂移”和长上下文性能崩溃。同时，长上下文建模面临结构性矛盾：**滑动窗口注意力** 维持低困惑度但无法捕捉远距离依赖；**全上下文注意力** 保留全局召回但遭受分布外困惑度爆炸。
- **整体含义**：论文试图在 **局部效率**（低困惑度）与 **全局记忆**（高召回率）之间取得平衡，提出一种混合架构 **ATMA**，通过解耦注意力的“内容”（方向）与“数量”（幅度），并引入压缩记忆，实现长度不变的语言建模。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：将注意力混合分解为三个独立的通道：
  - **方向通道（Content）**：输出单位向量，对计数不敏感，只表示“匹配了什么”。
  - **幅度通道（Count）**：基于有效匹配的参与比（inverse Simpson index），通过饱和映射（tanh）有界化。
  - **记忆通道（Memory）**：基于门控-德尔塔（Gated-Delta）快速权重规则的循环压缩记忆。
- **关键技术细节**：
  - **Polar Attention**：每个注意力头计算长度温度 `temp = 1 + softplus(α) · log(n_i)` 和极值校正空汇 `null = null_base + softplus(γ) · sqrt(log(n_i+1))`。方向通道：softmax 后取加权值并投影到单位球面。幅度通道：计算参与比 `n_eff = 1 / Σ w_hat^2`，再门控并映射到 `[0,1)`。总输出 `out_polar = W_o( c · gate ) + W_μ(mag)`。
  - **Titans Memory**：每头维持矩阵状态 `M ∈ R^{d_v × d_k}`，更新规则为 `M_t = γ_t M_{t-1}(I - β_t k_t k_t^T) + β_t v_t k_t^T`（等价于 Gated DeltaNet）。关键发现：**键归一化**（L2归一化）和**自稳定性**（状态范数保持平坦）。
  - **混合架构**：16层解码器，12层 LFM2 门控卷积 + 4层 Polar Attention（3:1比例）。每个注意力头使用组查询注意力（GQA: 8 查询头, 2 KV 头）。
- **算法流程**：每个时间步，输入通过 RMSNorm → 子层（卷积或 Polar Attention+记忆）→ 残差连接 → MLP（Squared ReLU）。Polar Attention 和记忆分支的输出相加后注入残差流。

#### 3. 实验设计
- **数据集与场景**：
  - **训练**：FineWeb-Edu 语料（1B tokens，序列长度 2048）。
  - **评估**：文档困惑度（FinePDFs 连贯文档）、诱导“大海捞针”（NIAH）检索准确率（植入形如 `The access code for record [KEY] is [D1] D2 D3 D4 D5]` 的针，测量贪婪逐位准确率）。
- **评估上下文长度**：从 1× 到 32× 训练长度（2K → 64K）。
- **对比方法**：
  - 注意力类型：**Polar**（本文）、**RoPE**（标准 softmax + 旋转位置编码）、**NoPE**（softmax 无位置编码）。
  - 记忆分支：**Titans Memory**（开/关）。
  - 滑动窗口（开/关）、干扰损失（开/关）、正则化模式（baseline/weak/strong/discrete/zipfian）。
- **基线比较**：除了内部消融，还比较了 11 个开源预训练模型（如 Qwen3.5-0.8B、SmolLM2-360M 等）。

#### 4. 资源与算力
- **GPU 型号**：NVIDIA L4 GPU（24GB 显存）。
- **训练规模**：每个配置约 370M 参数，训练 1B tokens（1 epoch）。
- **具体数量**：未明确说明使用的 GPU 卡数或训练时长。但报告了 **MFU**（模型浮点利用率）和平均步长时间。例如 Polar+Polar+Memory 配置 MFU 35.6%（平均每步 32.49s）。120 个配置总计训练开销未汇总。
- **训练 FLOPs**：估计约 2.2～2.3×10^18 FLOPs（根据 6ND 公式），相对较小。

#### 5. 实验数量与充分性
- **实验数量**：共 **120 次运行**（factorial ablation sweep），覆盖 5 个轴：注意类型（3）、正则化模式（5）、干扰损失（2）、记忆（2）、滑动窗口（2）。
- **充分性与公平性**：
  - **充分**：完整消融了每个组件的影响，包括组合效应。
  - **公平**：所有配置共享相同基础架构（层数、维度、训练超参数、优化器等），仅替换序列混合器。
  - **客观**：使用统一评估管道，指标清晰（困惑度、检索准确率、MFU）。
  - **局限**：仅在 1B token 小规模训练，与大规模预训练模型比较时存在训练预算巨大差异（表 4 显示 Qwen3.5-0.8B 等使用 10^21 FLOPs 级别，而 ATMA 仅 2.2×10^18 FLOPs）。论文明确说明这种比较是为了“锚定”而非直接竞争。

#### 6. 主要结论与发现
- 核心结论：**Polar Attention + Titans Memory 组合** 解决了长上下文困惑度-检索的权衡。
  - 在 64K token（32× 训练长度）上，检索准确率保持 **93%**（表 2），而 softmax+记忆基线降至 16%，RoPE+记忆降至 0%。
  - 文档困惑度单调下降（从 2.70 到 1.96 nats），表明模型主动利用更长上下文。
- 关键消融结果：
  - **独立组件不足**：纯 Polar Attention（无记忆）在 64K 时检索崩溃；softmax+记忆在长上下文时检索崩溃。
  - **窗口和干扰损失有害**：当记忆分支激活时，滑动窗口和干扰损失反而降低检索（表 3）。
  - **键归一化** 对 delta 记忆稳定性至关重要（Finding 1）。
- 与开源模型比较：Qwen3.5-0.8B 等大规模模型在 64K 上仍保持高检索和低困惑度，ATMA 受限于小训练预算，但架构自身证明其长度不变性。

#### 7. 优点
- **方法创新**：首次将注意力分解为方向/幅度/记忆三个正交通道，实现长度不变性。
  - 方向通道的 **单位向量投影** 切断计数影响。
  - 幅度通道的 **参与比+饱和映射** 防止激活漂移。
- **硬件协同设计**：实现 FlashAttention 风格 Triton 内核，在线计算参与比（O(block) 内存），GQA 分组的 paged decode 内核，以及 in-place gated-delta 步内核，显著降低开销（记忆分支仅增加 4.5% MFU 下降）。
- **深入消融**：120 组实验系统验证了每个组件的必要性，包括正则化模式、干扰损失、滑动窗口等。
- **开源复现**：提供完整代码链接。

#### 8. 不足与局限
- **训练规模有限**：仅 370M 参数和 1B tokens，远小于主流预训练模型（如 Qwen3.5 使用 36T tokens）。结论可能无法直接推广到更大规模。
- **任务覆盖单一**：评估仅基于文档困惑度和诱导 NIAH 任务，未涉及多样化的长上下文理解基准（如 MultiNEEDLE、LongBench、LV-Eval 等）。
- **与最先进模型的差距**：表 5 显示，在大规模预训练模型面前，ATMA 的绝对困惑度更高（例如 64K 时 2.83 bits/token vs. Qwen3.5 的 1.54 bits/token），表明其语言建模能力仍较弱。
- **记忆分支稳定性依赖**：键归一化是关键，但论文未讨论该操作对容量或表达能力的影响。
- **干扰损失的反作用**：在记忆激活时，干扰损失反而有害，说明过度的信号压制会削弱长期记忆效用。该机制可能需要更精细的调谐。
- **未提供更长上下文（>64K）的验证**：32× 训练长度已是极端，但实际应用可能需要 128K 或更长。

（完）
