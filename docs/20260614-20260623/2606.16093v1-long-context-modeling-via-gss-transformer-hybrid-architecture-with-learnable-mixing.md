---
title: Long-Context Modeling via GSS-Transformer Hybrid Architecture with Learnable Mixing
title_zh: 通过带有可学习混合机制的GSS-Transformer混合架构进行长上下文建模
authors: "Kuzey Torlak, Hüseyin Arda Arslan, Anıl Dervişoğlu, Beyza Nur Deniz, Onur Boyar"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16093v1"
tags: ["query:la"]
score: 9.0
evidence: 结合状态空间模型与注意力的混合架构，实现高效长上下文处理
tldr: "长上下文建模中，Transformer因平方级复杂度而效率低下，状态空间模型(SSM)虽线性但存在选择性召回瓶颈。为此提出并行混合架构PHA，将门控状态空间(GSS)、分组查询注意力(GQA)和前馈网络(FFN)作为独立并行分支，通过可学习混合机制融合。在WikiText-103上125M参数达16.51困惑度，优于Hedgehog和H3；180M参数困惑度16.42，同时吞吐量提升24%、长上下文内存降低40%。结果表明并行专家范式能以接近Transformer的困惑度实现显著效率提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16093v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 894, \"height\": 923, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16093v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 935, \"height\": 694, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16093v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1069, \"height\": 726, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16093v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1199, \"height\": 440, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16093v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 950, \"height\": 189, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16093v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1259, \"height\": 375, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16093v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 464, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16093v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1006, \"height\": 191, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16093v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1010, \"height\": 341, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16093v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1264, \"height\": 139, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16093v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1261, \"height\": 256, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16093v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1181, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16093v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1137, \"height\": 519, \"label\": \"Table\"}]"
motivation: Transformer自注意力平方复杂度低效，SSM线性但召回瓶颈，需平衡效率与困惑度。
method: 提出并行混合架构PHA：GSS捕获全局、GQA选择性检索、FFN补充处理，三者可学习混合。
result: "PHA 125M参数WikiText-103困惑度16.51，180M参数16.42，吞吐量提升24%，长上下文内存降低40%。"
conclusion: 分离序列建模为并行专家可达到Transformer级困惑度，并大幅提升长上下文效率。
---

## 摘要
建模长距离依赖关系仍然是自然语言处理中的一个核心挑战。Transformer架构通过自注意力机制实现了强大性能，但计算量随序列长度呈二次方增长（$O(N^2)$），而状态空间模型（SSM）的计算量虽为线性（$O(N)$），却存在选择性召回瓶颈，难以从压缩状态中精确检索信息。这就在效率与困惑度之间产生了基本权衡。为应对这些挑战，我们提出了并行混合架构（PHA），该架构将门控状态空间（GSS）、分组查询注意力（GQA）和前馈网络（FFN）作为独立的并行分支运行，并通过可学习的混合机制进行融合。PHA不强迫SSM近似注意力或将两种范式串行化，而是让每个分支实现专业化：GSS捕获全局上下文，注意力执行选择性检索，FFN提供互补处理。在WikiText-103上，PHA在125M参数下达到16.51 PPL，优于Hedgehog（16.70）和H3-125M（23.70）。扩展至180M参数后得到16.42 PPL，与纯注意力基线结果相当，同时在长上下文中吞吐量提高24%，内存使用降低高达40%。在OpenWebText上，我们的125M模型达到19.72 PPL，优于标准Transformer（20.60）和GSS混合基线（19.80）。这些结果表明，将序列建模范式分离为并行专家，能够在实现Transformer级别困惑度的同时，大幅提升长上下文语言建模的效率。

## Abstract
Modeling long-range dependencies remains a central challenge in natural language processing. Transformer architectures achieve strong performance via self-attention but scale quadratically ($O(N^2)$) with sequence length, while State Space Models (SSMs) scale linearly ($O(N)$) but suffer from a selective recall bottleneck, struggling to retrieve precise information from compressed states. This creates a fundamental tradeoff between efficiency and perplexity. To tackle these challenges, we propose the \textit{Parallel Hybrid Architecture (PHA)}, which runs Gated State Spaces (GSS), Grouped Query Attention (GQA), and Feed-Forward Networks (FFNs) as independent parallel branches fused by a learnable mixing mechanism. Instead of forcing SSMs to approximate attention or serializing the two paradigms, PHA allows each branch to specialize: GSS captures global context, while attention performs selective retrieval, with FFN providing complementary processing. On WikiText-103, PHA achieves 16.51 PPL at 125M parameters, outperforming Hedgehog (16.70) and H3-125M (23.70). Scaling to 180M parameters yields 16.42 PPL, which gives comparable results with the pure attention baseline while delivering 24\% higher throughput and up to 40\% lower memory usage at long contexts. On OpenWebText, our 125M model achieves 19.72 PPL, outperforming standard Transformers (20.60) and GSS hybrid baselines (19.80). These results demonstrate that separating sequence modeling paradigms into parallel specialists enables Transformer-level perplexity with substantially improved efficiency for long-context language modeling.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **核心矛盾**：Transformer 的自注意力机制计算量随序列长度平方增长（$O(N^2)$），在长上下文场景下效率低下；状态空间模型（SSM）虽具有线性复杂度（$O(N)$），但其固定大小的压缩状态会导致信息丢失，难以从远距离精确检索具体信息（选择性召回瓶颈）。
- **目标**：设计一种能在**保持近似 Transformer 困惑度的同时，大幅提升长上下文推理效率**的混合架构。
- **整体思路**：不再强制 SSM 模仿注意力或简单串行堆叠，而是将 GSS（门控状态空间）、GQA（分组查询注意力）和 FFN 作为**独立并行分支**，通过**可学习混合机制**融合各分支输出，让每个分支专攻其擅长的任务（全局建模、精确检索、特征处理）。

### 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：并行混合架构（PHA），输入经 RMSNorm 后同时送入三个分支：
  - **GSS 分支**：捕获全局上下文，使用门控递归更新状态，公式为  
    $h_t = \sigma(W_g x_t) \odot (A h_{t-1} + B x_t)$  
    其中 $A$ 使用 HiPPO 初始化，门控 $\sigma$ 控制信息流入状态。
  - **注意力分支**：执行选择性检索，使用 GQA（查询-键值头比例 4:1 或 3:1），RoPE 位置编码，并采用 **Scaled-Cosine Attention** 稳定训练、替代标准点积注意力。
  - **辅助 FFN 分支**：使用 SwiGLU 激活，扩展因子较小（1.25× 或 0.67×），用于补充处理。
- **融合机制**：可学习静态混合（Learnable Static Mixing）：三个标量参数 $p_{attn}, p_{ssm}, p_{ffn}$ 经 Softplus 变换后归一化为权重 $w_i$，对每个分支输出先独立 RMSNorm 再加权求和。  
  $w_i = \frac{\text{Softplus}(p_i)}{\sum_j \text{Softplus}(p_j)},\quad y_{mix} = \sum_k w_k \cdot \text{RMSNorm}(z_k)$
- **稳定训练**：采用 DeepNorm 残差缩放，系数 $\alpha = (2L)^{0.25}$，避免复杂学习率预热。
- **整体流程**：输入 → RMSNorm → 三并行分支 → 可学习混合 → 主 FFN（SwiGLU, 扩展因子 2.5× 或 2×） → 输出。

### 3. 实验设计

- **数据集**：
  - **WikiText-103**：完整 Wikipedia 文章，上下文长度 1024，GPT-2 BPE 分词。
  - **OpenWebText**：从 Reddit 链接收集的网页文档，文档级切分，验证/测试各 0.5%。
- **Benchmark 指标**：Perplexity（PPL，越低越好），另报告准确率（Accuracy）。
- **对比方法**：
  - 同类混合架构：H3-125M、H3-355M、Hymba（仅提及）、GSS Hybrid。
  - 近似线性注意力：Hedgehog、Linear Transformer、CosFormer。
  - 循环/压缩注意力：Transformer-XL、Compressive Transformer。
  - 纯 SSM/卷积：S4、Hyena、AWD-QRNN。
  - 标准 Transformer：GPT-2 Small、ALiBi、Adaptive Input、Routing Transformer（稀疏注意力）。
- **模型配置**：三个规模（90M、125M、180M），关键参数如 $d_{model}$、层数、头数、FFN 扩展比见 Table 1。

### 4. 资源与算力

- **硬件**：单张 NVIDIA A100 GPU。
- **框架**：PyTorch + Hugging Face Accelerate，全精度 fp32。
- **训练方案**：
  - WikiText-103：12 epochs，AdamW，ReduceLROnPlateau 调度，batch size 24。
  - OpenWebText：1 epoch，cosine 学习率调度（2000 warmup steps），batch size 24，总 token 数约 12B（对比基线约 50B）。
- **未明确**：总训练时间（小时级别未给出），但可从 batch size 和序列长度估算。

### 5. 实验数量与充分性

- **总实验组数**：
  - WikiText-103 上三个规模（90M/125M/180M），125M 和 180M 分别用 5 种子和 3 种子（具体种子数见表 2 脚注），报告均值±标准差。
  - OpenWebText 上仅 125M 模型 1 种子（但对比基线已报告）。
  - 额外消融实验：5 组（并行 vs 串行、分支移除 4 种、混合权重分析）。
- **充分性**：
  - **消融全面**：覆盖并行 vs 串行、每个分支贡献、混合权重行为。
  - **对比公平**：相同数据集、分词器、参数规模（但部分基线使用不同长度/分词器，作者已说明）。
  - **统计鲁棒**：多种子报告标准差。
  - **局限**：OpenWebText 训练 token 数仅为基线的 23%，虽凸显样本效率，但直接比较 PPL 时需谨慎。此外，未在更多长上下文任务（如摘要、问答）上验证。

### 6. 主要结论与发现

1. **性能优势**：PHA-125M 在 WikiText-103 上 PPL 16.51，显著优于 Hedgehog（16.70）和 H3-125M（23.70）；PHA-180M 达到 16.42，接近 Routing Transformer（15.80，但参数更多）。
2. **效率优势**：在长上下文（8192 tokens）下，PHA-180M 对比纯 Attention 模型吞吐量提升 24%，内存降低 22%~40%。
3. **并行优于串行**：相同组件下并行设计比串行设计 PPL 更低（16.42 vs 16.46），且参数更少。
4. **分支互补**：消融表明注意力是检索关键（移除后 PPL 暴涨 4.27），GSS 承担大部分全局建模（平均权重 46.9%），辅助 FFN 提供补充（21.9%）。
5. **混合权重的 "Sandwich" 模式**：GSS 在输入层和输出层权重最大，注意力在中间层活跃，显示分层专业化。

### 7. 优点

- **创新简洁**：并行分支 + 可学习标量混合，避免了复杂路由或特殊头部设计，易于实现。
- **消融彻底**：系统性地验证了并行性、各分支必要性及混合权重行为。
- **效率分析扎实**：提供不同序列长度下的吞吐量和峰值内存对比，证明实际部署收益。
- **多规模验证**：90M~180M 展示一致缩放特性，且与更大参数模型竞争。
- **样本效率提示**：在 OpenWebText 上用更少 tokens 达到 competitive PPL，暗示架构可能更高效利用数据。

### 8. 不足与局限

- **实验覆盖不足**：仅评估语言建模 PPL，未扩展到下游任务（如长文档问答、摘要、代码生成），也未进行“大海捞针”测试来验证长距离检索能力。
- **基线对比不完全**：未与同期最先进的 SSM 变体（如 Mamba-2、Jamba）直接比较；部分基线使用不同分词器（word-level vs BPE），虽然作者提及但数值对比仍存偏差。
- **训练配置差异**：OpenWebText 实验 token 量远少于基线，虽然强调样本效率，但无法直接判定是架构优势还是运气因素，需更严格等 token 实验。
- **可扩展性存疑**：最大模型仅 180M，未在更大规模（如 1B+）验证，混合权重模式可能随深度变化。
- **混合机制分析深度**：论文展示“Sandwich”模式但未讨论其是否依赖特定超参数或随机初始化；Scaled-Cosine Attention 对稳定的贡献未单独消融。
- **代码未公开**：论文未提及开源计划，降低可复现性。

（完）
