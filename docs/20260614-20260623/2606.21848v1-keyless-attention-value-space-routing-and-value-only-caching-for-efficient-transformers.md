---
title: "Keyless Attention: Value-Space Routing and Value-Only Caching for Efficient Transformers"
title_zh: 无键注意力：用于高效Transformer的值空间路由与仅值缓存
authors: Xin Gao
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21848v1"
tags: ["query:la"]
score: 9.0
evidence: 无键注意力与值缓存实现高效Transformer
tldr: "标准Transformer中KV缓存内存开销大。提出Keyless Attention，完全消除key投影，仅使用query和value，实现Value-Only缓存，将KV缓存内存减少50%，同时解码吞吐不变。引入深度-m注意力分解框架，Keyless Attention为m=3实例。在5个模型上，4个模型困惑度匹配或超越标准注意力；GPT-2 557M在4/5常识推理基准上更优。该方法有效降低缓存开销，性能不减。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21848v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1024, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21848v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1028, \"height\": 622, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21848v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1706, \"height\": 287, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21848v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1697, \"height\": 683, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21848v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1369, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21848v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1448, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21848v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 817, \"height\": 537, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21848v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1033, \"height\": 419, \"label\": \"Table\"}]"
motivation: 标准Transformer中KV缓存占用大量内存和访问带宽，尤其在长序列生成时成为瓶颈。
method: 消除key投影，仅使用query和value，通过值空间路由矩阵实现Keyless Attention，并引入深度-m注意力分解理论。
result: 在GPT-2、Pythia、Qwen2等5个模型上，4个模型困惑度匹配或超越标准注意力，GPT-2 557M在4/5常识推理基准上更优，KV缓存减半，解码吞吐相当。
conclusion: Keyless Attention通过去除key投影实现高效缓存，性能未降，为长序列Transformer部署提供了可行方案。
---

## 摘要
我们提出无键注意力（Keyless Attention），一种完全消除键投影的注意力机制，仅对查询和值进行操作。这产生了仅值缓存（Value-Only Cache），与标准注意力相比，KV缓存内存和访问开销减少50%，同时匹配或超越标准注意力的解码吞吐量。除了高效性之外，我们引入深度-m注意力分解：标准注意力计算注意力双线性形式的深度-2分解，而无键注意力实现了该系列的深度-m实例。在m=3时，无键注意力通过一个值空间路由矩阵匹配标准注意力的投影矩阵数量，该矩阵替换了键投影，并引入了路由与检索之间的耦合。在五个模型和四种架构（GPT-2 280M、GPT-2 557M、Pythia 410M、Qwen2 1.5B和Llama 3.2 1B）上的实验表明，无键注意力在5个模型中的4个上，在困惑度方面匹配或优于标准QKV注意力。在下游零样本评估（GPT-2 557M）中，无键注意力在5个常识推理基准中的4个上表现更优，同时在整个过程中实现了50%的KV缓存减少。

## Abstract
We propose Keyless Attention, an attention mechanism that eliminates the key projection entirely, operating over queries and values only. This yields a Value-Only Cache that reduces KV cache memory and access overhead by exactly 50% over standard attention, while matching or exceeding standard attention's decode throughput. Beyond efficiency, we introduce Depth-$m$ Attention Factorization: standard attention computes a depth-2 factorization of the attention bilinear form, while Keyless Attention realizes a depth-$m$ instance of this family. At m=3, Keyless Attention matches the projection matrix count of standard attention via a value-space routing matrix that replaces the key projection and introduces a coupling between routing and retrieval. Experiments across five models and four architectures (GPT-2 280M, GPT-2 557M, Pythia 410M, Qwen2 1.5B, and Llama 3.2 1B) show that Keyless Attention matches or outperforms standard QKV attention on perplexity in 4 out of 5 models. On downstream zero-shot evaluation (GPT-2 557M), Keyless Attention outperforms on 4 out of 5 commonsense reasoning benchmarks, while achieving 50% KV cache reduction throughout.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：标准Transformer在自回归推理时依赖KV缓存存储键和值，随着序列长度和层数增加，KV缓存内存和带宽成为主要瓶颈，现有方法（如量化、剪枝、系统级优化）大多“事后”优化缓存，未质疑键投影的必要性。  
- **整体含义**：论文挑战了注意力机制中需要显式键投影的基本假设，提出完全消除键投影的无键注意力（Keyless Attention），仅使用查询和值，实现Value-Only缓存，将KV缓存内存减少50%而不损失性能。同时引入深度-m注意力分解理论，重新参数化注意力双线性形式。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：用值空间路由替代键空间路由。标准注意力使用Q、K、V三个投影，其中K与V独立存储；无键注意力直接计算Q与V的点积作为注意力分数，消除K投影，从而只需缓存V。
- **关键公式**：标准注意力：`Attn = softmax(QK^T/√dk)V`；无键注意力：`Attn = softmax(QV^T/√dk)V`，其中Q = X W_Q，V = X W_V，W_Q和W_V为可学习矩阵。
- **值空间路由**：通过组合W_Q = W_{Q1} W_{Q2}实现深度-3（m=3）分解，匹配标准注意力的投影矩阵数量，同时W_{Q2}将查询映射到值空间，使路由与检索耦合。
- **深度-m注意力分解**：将注意力双线性形式的参数化视为深度-m因式分解（标准QKV为m=2，Keyless Attention QVV(3)为m=3），m为可调超参数。
- **推理优化**：训练时W_{Q1}和W_{Q2}分开学习，推理时可预乘融合为单个矩阵，消除额外计算开销；Value-Only缓存只存储V，内存减半。
- **与现有方法兼容**：可结合MQA、GQA、MLA等，进一步压缩缓存。

### 3. 实验设计：数据集、基准、对比方法
- **数据集**：语言建模使用WikiText-103（30M token子集，受限于GPU内存）。零样本常识推理使用HellaSwag、ARC-Challenge、BoolQ、SciQ、StoryCloze。
- **基准与对比**：主要对比标准QKV注意力。模型包括GPT-2 280M、GPT-2 557M、Pythia 410M、Qwen2 1.5B、Llama 3.2 1B，覆盖三种位置编码（绝对、部分RoPE、全RoPE）、两种残差设计（顺序、并行）、两种注意力分组（MHA、GQA）。
- **评价指标**：验证集困惑度（PPL）、零样本准确率、KV缓存大小、解码吞吐量。

### 4. 资源与算力
- 论文明确说明：所有实验使用**单张NVIDIA A100-SXM4-80GB GPU**。训练时长未具体给出，但提到使用30M token子集（受GPU内存限制）。GPT-2模型训练使用线性学习率调度（5% warmup），其他模型使用余弦衰减（5% warmup），各训练4个epoch（图3所示）。总体算力需求适中。

### 5. 实验数量与充分性
- **实验数量**：共5个模型（含3个公开架构）+ GPT-2两个深度，每组3个随机种子报告均值和标准差。零样本评估5个基准，消融实验比较不同分解深度（m=2,3,4,4+ReLU），以及QKV(3)对比。还测量了推理吞吐量和缓存大小。
- **充分性与客观性**：
  - 跨架构验证充分（涵盖主流Transformer变体）。
  - 条件可控：仅改变注意力机制，其余超参数、训练流程、数据完全相同。
  - 统计报告标准差，显示结果稳定。
  - 不足：仅使用WikiText-103单一训练集，未在更大或更多样化语料（如C4、The Pile）上验证；零样本基准规模有限（5个），推理吞吐量仅测batch size=1。
  - 总体公平，但可进一步扩展。

### 6. 论文的主要结论与发现
- Keyless Attention在4/5模型上困惑度匹配或优于标准QKV注意力（GPT-2 557M、Pythia 410M、Qwen2 1.5B、Llama 3.2 1B胜出；GPT-2 280M持平）。
- 零样本推理：在5个基准中的4个上准确率更高（HellaSwag +0.9%，StoryCloze +1.9%），SciQ略低2-3点。
- KV缓存内存严格减半（从2倍到1倍），解码吞吐量在长上下文时略有提升（因消除键投影）。
- 梯度纠缠（W_{Q2}与W_V耦合）起到隐式正则化作用，使模型过拟合更慢，泛化更好。
- 深度m=3（QVV(3)）在效率与性能间最佳，且可推广到cross-attention和多模态场景。

### 7. 优点：方法或实验设计上的亮点
- **方法创新**：从根源减少缓存，而非事后压缩；理论证明标准注意力可被Keyless Attention等价表示（给定满秩条件），消除了对键存储的需求。
- **统一框架**：深度-m注意力分解将多种注意力机制（Luong、QKV、Keyless）纳入统一视角，m作为可调超参数。
- **实验设计**：跨多种架构（位置编码、分组策略、残差设计）验证，增强结论普适性；报告均值与标准差，消融深度m，测量实际吞吐量。
- **实用兼容性**：可直接替换标准注意力，与MQA/GQA/MLA等结合，无额外假设。

### 8. 不足与局限
- **数据集与任务覆盖有限**：仅用WikiText-103训练，未在更大、更多元语料（如C4、The Pile）或长上下文（如128k+）场景验证。下游仅5个基准，且不包含对话、代码、数学等复杂任务。
- **长序列推理**：虽测量了8k上下文，但未深入测试更极端长度（如32k+）下的缓存性能与注意力退化。
- **资源与训练时长**：论文未报告具体训练时间、收敛步数，以及不同模型训练计算成本对比。
- **理论假设**：等价性定理需要W_V满列秩，实践中可能不严格满足，但论文声明Keyless Attention独立有效。
- **SciQ下降约2-3点**：表明在事实检索任务上可能稍有损失，原因待分析。
- **工业部署验证不足**：仅在单GPU上做小批量测试，未在大批量、分布式推理场景下评估。

（完）
