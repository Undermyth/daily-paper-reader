---
title: "COBS: Cumulant Order Block Sparse Attention"
title_zh: "COBS: 累积量阶块稀疏注意力"
authors: "Alexander Tian, Aditya Ghai, Sanjit Neelam, Zaal Vasania, Akshay Mishra"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09052v1"
tags: ["query:la"]
score: 8.0
evidence: 块稀疏注意力用于高效KV缓存读取
tldr: "块稀疏注意力可缓解长上下文LLM的KV缓存读取瓶颈，但精确的块选择需读取所有key。现有方法通过一阶近似估计注意力质量，但精度不足。本文提出COBS，基于累积量展开存储压缩二阶统计量，实现更准确的块选择。在32k RULER长上下文检索中，COBS将NSA基线从0.2999提升至0.8195，接近密集注意力的0.9040，缩小了约86%的差距，同时KV读取流量仅为NSA基线的1.21倍和密集注意力的1/15.15。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09052v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1527, \"height\": 830, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09052v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1536, \"height\": 601, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09052v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1614, \"height\": 596, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09052v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1624, \"height\": 657, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09052v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1163, \"height\": 650, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09052v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1627, \"height\": 468, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09052v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1650, \"height\": 557, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09052v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1336, \"height\": 358, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09052v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1653, \"height\": 531, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09052v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 774, \"height\": 319, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09052v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1392, \"height\": 353, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09052v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1329, \"height\": 158, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09052v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1331, \"height\": 334, \"label\": \"Table\"}]"
motivation: 现有块稀疏注意力选择策略因依赖一阶近似而质量不佳。
method: 通过累积量展开提出COBS，存储压缩二阶统计量改进块选择。
result: "在32k RULER上COBS达0.8195，缩小与密集注意力86%差距，仅用1.21倍KV读取。"
conclusion: COBS验证了利用高阶信息进行高效块稀疏注意力的有效性。
---

## 摘要
块稀疏注意力是一种硬件友好的方式，用于缓解大型语言模型（LLM）中的键值（KV）缓存读取瓶颈。然而，它在领先的开源LLM中并不普遍，这些模型更依赖密集注意力或细粒度选择，这促使我们进行分析。我们以DeepSeek的Native Sparse Attention（NSA）为代表方法进行研究，其三分支设计使我们能够隔离块选择这一最具挑战性和关键性的阶段。我们形式化了选择过程，并将其简化为根据单一量（注意力质量，即一个块内注意力分数的总和）对块进行排序。我们证明，如果选择检索到具有最大注意力质量的块，则块稀疏注意力能够匹配密集注意力的质量。然而，计算精确的注意力质量需要读取每个键，因此块选择问题最终归结为通过紧凑摘要而非完整键来近似该质量。通过累积量展开，我们揭示了现有方法失效的原因：它们的选择策略试图估计注意力质量，但局限于一阶近似。因此，我们提出了COBS（累积量阶块稀疏注意力），这是一种基于NSA的注意力方法，并引入了一个新颖的选择器，该选择器为每个块存储压缩的二阶统计量。在32k RULER长上下文检索基准上，COBS将NSA基线的平均分数从0.2999提高到0.8195，接近密集注意力的0.9040，缩小了约86%的差距，同时仅使用NSA基线1.21倍的KV缓存读取流量，且比密集注意力少15.15倍的读取流量。相同的模型在短上下文行为中表现良好，并且在我们的比较中获得了比密集注意力更低的位置负对数似然（NLL）。

## Abstract
Block sparse attention is a hardware friendly way to alleviate the key-value (KV) cache read bottleneck in large language models (LLMs). However, it is not prevalent among leading open-weight LLMs, which rely instead on dense attention or fine-grained selection, thereby motivating our analysis. We study DeepSeek's Native Sparse Attention (NSA) as a representative method, whose three-branch design lets us isolate block selection, the most challenging and consequential stage. We formalize selection and reduce it to ranking blocks by a single quantity, the attention mass: the sum of a block's attention scores. We show that if selection retrieves the blocks with the largest attention mass, block sparse attention can match the quality of dense attention. However, computing the exact attention mass requires reading every key, so the problem of block selection ultimately reduces to approximating this mass from a compact summary instead of the full keys. Via a cumulant expansion, we show why existing methods falter: their selection strategies attempt to estimate the attention mass, but are confined to a first-order approximation. Therefore, we propose COBS (Cumulant Order Block Sparse Attention), an attention method that builds on NSA, incorporating a novel selector that stores a compressed second-order statistic per block. On the 32k RULER long-context retrieval benchmark, COBS raises the NSA baseline's mean score from 0.2999 to 0.8195, approaching dense attention at 0.9040 and closing about 86% of the gap, while using only 1.21x the KV cache read traffic of the NSA baseline and 15.15x less read traffic than dense. The same model preserves short-context behavior and attains lower position-wise negative log-likelihood (NLL) than dense attention in our comparison.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机与背景）

- **问题**：大语言模型（LLM）在长上下文推理时，**KV 缓存读取**成为主要瓶颈（受限于内存带宽而非计算）。稀疏注意力通过仅读取部分 token 的 KV 来降低成本，其中**块稀疏注意力**因连续内存访问和粗粒度 top-k 选择而尤为硬件友好。
- **现状**：但领先的开源 LLM 大多采用密集注意力、细粒度 token 选择或近似方法，**块稀疏方法并未普及**。已有方法（如 NSA）的块选择器仅使用一阶近似估计注意力质量，精度不足，导致与密集注意力存在显著差距。
- **目标**：揭示块选择问题的本质，提出能精确估计注意力质量的**可缓存**选择器，从而在接近密集注意力性能的同时大幅降低 KV 读取流量。

### 论文提出的方法论

#### 核心思想
1. **形式化块选择**：将选择简化为按注意力质量（块内 softmax 分数之和）排序。若能用精确质量排序（Oracle Sparse Attention，OSA），几乎可匹配密集注意力。
2. **累积量展开**：块质量的对数为「块内 key 分布」的累积量生成函数（CGF）加常数，CGF 的泰勒展开为：
   ```
   ln mb = ln L + q^T κ1 + (1/2) q^T κ2 q + 高阶项
   ```
   其中 κ1 = 块均值键，κ2 = 块内键协方差矩阵。**现有一阶选择器仅捕获 q^T κ1，忽略了关键的 (1/2) q^T Σ q 项。**
3. **COBS 方案**：在 NSA 基础上，为每个块存储压缩的二阶统计量（协方差），使得选择器能计算二阶项。

#### 关键技术细节
- **NoPE**：移除压缩和选择分支中的旋转位置编码（RoPE），使块摘要仅基于内容，避免位置干扰。
- **协方差存储与压缩**：
  - 每块存储均值键 (κ1) 和低秩分解的协方差 (Σ ≈ ∑ λ_i u_i u_i^T, 保留 top r 特征向量)。
  - **子空间投影**：将协方差投影到查询子空间（维度 s），降低每块存储量（从 rD → rs）。
  - **量化**：存储的特征向量量化为 FP4（E2M1），块均值保留 bf16，进一步减少存储。
- **评分公式**：解码时，对每个查询 q，投影到子空间得到 q̃，然后计算：
  ```
  ln m̂_b = ln L + q̃^T k̄_b + (1/2) q̃^T B_b q̃
  ```
  其中 B_b 是子空间内协方差，实际用 r 个缩放特征向量计算：q̃^T B_b q̃ ≈ ∑_{i=1}^r (ξ_i^T q̃)^2。

### 实验设计

- **数据集与场景**：
  - **主要基准**：**RULER** 长上下文检索（32k 长度，11 个任务：单/多键检索、多查询、多值、常见词/频繁词提取、变量跟踪等）。
  - **短上下文常识推理**：OpenBookQA、PIQA、HellaSwag、ARC、TriviaQA、WinoGrande（零样本）。
  - **语言建模**：在 LongCrawl64 自然长文本上评估位置级 NLL。
- **对比方法**：
  - Dense attention（全注意力）
  - OSA（Oracle Sparse Attention，使用真实质量排序，仅用于诊断）
  - NSA MLP（原始 NSA 基线，使用 MLP 池化摘要）
  - NSA mean-pool（用块均值键作为摘要）
  - NSA Quest（采用 Quest 的按轴 min/max 范围评分）
  - COBS 变体（full-space r=4/6, subspace s≈85 r=4 FP4 等）
- **模型与训练**：
  - 约 1.2B 参数（NSA MLP 为约 1.7B），16 层，GQA-4，头维度 128。
  - 预训练：LongCrawl64 约 20B tokens（NSA MLP 约 28B）。
  - 上下文扩展：YaRN 至 32k，然后对 RULER 风格数据 SFT。
  - 超参数：块大小 L=32，top-k=16，滑动窗口 256。

### 资源与算力

- **文中未明确说明 GPU 型号、数量或训练时长**。
- 提及使用 **seqax（基于 JAX 的 LLM 代码库）**，基础设施由 MatX 团队维护。
- 预训练规模：约 20B tokens（多数变体），推断资源消耗主要集中在 KV 读取流量分析（表 3）。

### 实验数量与充分性

- **实验数量**：大量消融和对比，包括主要比较（表 1、图 3）、常识推理（表 2）、位置 NLL（图 4）、KV 流量（表 3/图 1）、秩扫描（图 5）、子空间维度（表 4、图 6）、量化（表 5）、查询中心展开（表 6）、偏度校正（表 7），以及负面结果报告。
- **公平性**：所有块稀疏方法在相同 NSA 框架下比较，选择分支外部分一致；NSA MLP 因参数量更大而分配了更多预训练 token（约 28B vs 20B），已做出一定补偿，但仍非完全公平。
- **充分性**：覆盖了长上下文检索、短上下文、语言建模、消融等，但**未在大规模模型（如 7B/70B）上验证**，也**未提供端到端推理速度对比**（仅有流量分析）。

### 论文的主要结论与发现

1. **一阶选择器本质局限**：理论上，块选择的最佳准则是注意力质量，精确排序可接近密集注意力（OSA 达 0.9010 vs 密集 0.9040）。但现有方法仅捕捉一阶项（均值），缺失二阶曲率。
2. **COBS 显著缩小差距**：子空间 FP4 配置在 32k RULER 上达到 **0.8195**，缩小了 NSA MLP（0.2999）到密集（0.9040）差距的 **86%**。
3. **效率优势**：KV 读取流量仅为 NSA 基线的 **1.21×**，比密集少 **15.15×**。
4. **短上下文无损**：短上下文常识推理与密集持平，位置 NLL 甚至低于密集（1.633 vs 1.727），表明长上下文能力提升不损害短文本。
5. **高秩回归**：存储的协方差秩超过 8 后性能退化（多键检索任务下降），归因于符号不可知的方差项可能放大干扰块。

### 优点

- **理论扎实**：从累积量展开严格推导出一阶局限，并给出二阶修正的方案，方法具有清晰直觉和数学基础。
- **硬件友好**：保持了块稀疏的连续内存访问优势，存储紧凑（低秩+子空间+FP4），开销可控。
- **实验设计清晰**：通过 OSA 作为 oracle 证明了一阶选择器改善空间，实验对比了多种经典选择器，消融充分。
- **负面结果公开**：报告了秩回归、廉价偏度、查询中心展开等失败尝试，体现诚实性和对方法的深入理解。

### 不足与局限

- **规模有限**：仅在约 1.2B 模型上验证，未在更大主流模型（如 7B、70B）上测试，泛化性未知。
- **NSA 比较不完全公平**：超参数（重叠/非重叠块）、压缩分支实现与原文 NSA 有差异，且 NoPE 仅用于稀疏变体，密集基线仍用 RoPE，造成混淆因素。
- **RULER SFT 非标准**：微调数据为生成式 RULER 风格数据，可能使模型偏向此类任务，影响真实长上下文泛化。
- **KV 流量 ≠ 实际速度**：分析仅基于读取字节数，未涉及核函数优化、批处理、硬件平台影响，不保证端到端加速。
- **高秩回归问题未完全解决**：虽识别了原因（未符号的方差项），但未提出稳定方法；廉价偏度校正仅在特定秩下有效，且增加额外开销。
- **未与最新方法（如 CCQ、DSA、HCA）直接对比**：仅与 NSA 系列和 Quest 进行了比较。

（完）
