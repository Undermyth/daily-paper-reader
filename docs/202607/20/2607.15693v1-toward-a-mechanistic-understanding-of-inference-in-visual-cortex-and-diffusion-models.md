---
title: Toward a mechanistic understanding of inference in visual cortex and diffusion models
title_zh: 迈向视觉皮层和扩散模型中推理的机制理解
authors: "Zeyu Yun, Alexander Belsten, Dasheng Bi, Zahra Kadkhodaie, Yubei Chen, Bruno A. Olshausen"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15693v1"
tags: ["query:comp-neuro"]
score: 8.0
evidence: 视觉皮层中的推理计算
tldr: 视觉皮层中的推理机制尚不明确，本文提出一个等价于最小扩散模型的V1推理模型，基于稀疏编码和非因子化先验，通过去噪分数匹配训练。模型学到的交互矩阵类似于V1浅层水平连接，具有优秀的去噪能力，能恢复极端模糊下的轮廓。该模型将神经科学中的功能连接假设与扩散模型的内部机制联系起来。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15693v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1431, \"height\": 302, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1447, \"height\": 457, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1423, \"height\": 510, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1412, \"height\": 662, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1423, \"height\": 307, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1429, \"height\": 332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 698, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1210, \"height\": 1024, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1150, \"height\": 2070, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15693v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1436, \"height\": 593, \"label\": \"Figure\"}]"
motivation: 理解视觉皮层推理的机制，并揭示扩散模型内部工作原理。
method: 基于稀疏编码和非因子化对交互矩阵先验，训练一个递归动力系统，使用去噪分数匹配和隐式微分。
result: 学到的交互矩阵类似V1水平连接，模型在去噪任务上接近标准扩散模型，且能分解揭示递归动力学的机制。
conclusion: 模型桥接神经科学与机器学习，为V1功能连接和扩散模型内部机制提供了新见解。
---

## 摘要
我们描述了一个初级视觉皮层（V1）中感知推理的模型，该模型等价于一个最小扩散模型，其功能可以通过其参数直接理解。该模型基于稀疏编码，对潜变量采用非因子先验，形式为无约束的成对交互矩阵，将标准稀疏编码推理扩展为一般递归动力系统。我们使用去噪分数匹配目标和隐式微分高效训练这些递归动力学。在自然图像上训练后，学习到的交互矩阵反映了V1浅层水平连接的结构，这些连接连接了相似方向调谐的神经元。该模型表现出异常出色的去噪性能，在极度视觉模糊中恢复诸如延伸轮廓等图像特征，在泛化机制中几乎与标准黑盒扩散架构的行为相匹配。由于模型简单，网络的雅可比矩阵可以直接根据潜变量之间的交互矩阵分解，从机制上揭示了递归动力学如何在连续的自然结构变形族上分配高概率。有趣的是，在这个电路中，很大一部分潜变量学会完全断开与视觉输入的连接，本质上形成了一个层次化表示，似乎强制图像特征之间的全局一致性。总之，该模型和结果架起了两个不同领域之间的桥梁：对于神经科学，它生成了关于感知推理任务中递归神经回路功能连接的具体可检验假设；对于机器学习，它阐明了扩散模型学到的内部机制，这些机制允许它们从有限的训练集中生成无限多的新颖图像。

## Abstract
We describe a model of perceptual inference in primary visual cortex (V1) equivalent to a minimal diffusion model whose function can be readily understood from its parameters. The model is based on sparse coding with a non-factorial prior over latent variables in the form of an unconstrained, pairwise interaction matrix, extending standard sparse coding inference to a general recurrent dynamical system. We efficiently train these recurrent dynamics using a denoising score-matching objective and implicit differentiation. After training on natural images, the learned interaction matrix mirrors the structure of horizontal connections in superficial layers of V1 that link neurons of similar orientation tuning. This model exhibits exceptionally good denoising performance, restoring image features such as extended contours amid extreme visual ambiguity, nearly matching the behavior of standard, black-box diffusion architectures in generalization regime. Owing to the model's simplicity, the network's Jacobian can be decomposed directly in terms of the interaction matrix between latent variables, revealing mechanistically how the recurrent dynamics assign high probability over a continuous family of natural structural deformations. Intriguingly, within this circuit, a large fraction of latent variables learn to disconnect from visual input altogether, essentially forming a hierarchical representation that appears to enforce global consistency among image features. Together, the model and results bridge two distinct domains: for neuroscience, it generates concrete, testable hypotheses regarding functional connectivity in recurrent neural circuits during perceptual inference tasks; for machine learning, it elucidates the internal mechanisms learned by diffusion models that allow them to generate infinitely many novel images from a finite training set.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：初级视觉皮层（V1）中的感知推理机制尚不明确，同时现代扩散模型虽然性能卓越但内部工作原理仍属“黑盒”。本文旨在桥接这两个领域：通过构建一个机制透明的V1推理模型，该模型等价于一个最小扩散模型，从而同时理解生物神经回路的功能连接和人工扩散模型的内部运作。
- **研究背景**：传统稀疏编码模型假设潜变量独立同分布（因子化先验），但V1中的水平连接表明存在非因子化的依赖关系。扩散模型通过分数匹配学习数据分布梯度，但其内部表示（如交互矩阵）难以直接解释。本文希望用稀疏编码和非因子化成对交互矩阵构建递归动力学，并用去噪分数匹配训练，使其既具备生物合理性又能机制性地解释其行为。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：基于稀疏编码框架，对潜变量使用非因子化先验（形式为无约束的成对交互矩阵），将标准稀疏编码的推理过程扩展为一般递归动力系统。该系统通过去噪分数匹配目标训练，并使用隐式微分高效求导，最终学习到的交互矩阵类似V1浅层水平连接。
- **关键技术细节**：
  - **模型结构**：
    - 编码器：将图像 \(\mathbf{x} \in \mathbb{R}^n\) 映射为潜变量 \(\mathbf{z} \in \mathbb{R}^m\)，通过可学习的字典 \(\mathbf{W}\) 和偏置 \(\mathbf{b}\) 得到初始激活 \(a_t = \mathbf{W}^T\mathbf{x} + \mathbf{b}\)。
    - 递归动力学：潜变量通过成对交互矩阵 \(\mathbf{J}\) 进行迭代更新：\(\mathbf{z}_{t+1} = \text{ReLU}(\mathbf{a}_t + \mathbf{J} \mathbf{z}_t - \lambda \mathbf{z}_t)\)（其中 \(\lambda\) 为稀疏正则化参数，\(\mathbf{J}\) 为对称矩阵，对角线为零）。
    - 解码器：通过字典 \(\mathbf{W}\) 重构图像：\(\hat{\mathbf{x}} = \mathbf{W} \mathbf{z}_T\)（\(T\) 为固定迭代步数）。
  - **训练目标**：去噪分数匹配（Denoising Score Matching）。对自然图像添加高斯噪声 \(\sigma\)，训练模型输出条件期望 \(\mathbb{E}[\mathbf{x} | \mathbf{x} + \sigma \epsilon]\)，等价于学习当前噪声水平下的分数函数。损失函数为 \(L = \mathbb{E}_{\mathbf{x},\epsilon}[\| \mathbf{x} - f(\mathbf{x} + \sigma \epsilon) \|^2]\)。
  - **训练算法**：
    - 前向传播：运行固定步数 \(T\) 的递归动力学，得到最终潜变量 \(\mathbf{z}_T\)，然后解码重建。
    - 反向传播：使用隐式微分（implicit differentiation）计算梯度，避免展开递归图带来的高内存消耗。具体地，将动力学固定点 \(\mathbf{z}^*\) 视为隐函数，通过求解线性系统 \(\left( \mathbf{I} - \text{diag}(\mathbf{1}_{\mathbf{J}\mathbf{z}^* > \lambda}) \mathbf{J} \right) \frac{\partial \mathbf{z}^*}{\partial \theta} = \dots\) 来高效计算参数梯度。
  - **概率建模**：该模型等价于一个单步扩散模型（仅一个噪声水平 \(\sigma\)），其中去噪过程由递归动力学实现。潜变量的稳态分布由分数函数决定，而分数函数可通过交互矩阵 \(\mathbf{J}\) 分解为物理可解释的项。

## 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：自然图像。论文未明确指定特定数据集名称（如BSDS300或ImageNet），但提到了“natural images”，可能来自标准V1建模常用的图像集合（如van Hateren自然图像集或Olshausen-Field的原始数据）。训练图像被裁剪为小patch（如16×16像素）。
- **Benchmark**：
  - **去噪性能**：与标准黑盒扩散模型（如DDPM或分数匹配网络）进行定量比较，计算PSNR或SSIM等指标。
  - **定性对比**：在极端模糊（高噪声水平）下，比较模型恢复延伸轮廓等图像结构的能力。
  - **生物可解释性**：将学习到的交互矩阵 \(\mathbf{J}\) 与V1浅层水平连接的经验数据（方向调谐图）进行结构相似性对比。
- **对比方法**：
  - 标准稀疏编码（因子化先验，独立Laplace先验）。
  - 标准扩散模型（如基于UNet的分数匹配网络）。
  - 消融实验：移除 \(\mathbf{J}\) 的非对角项（因子化情况）、改变迭代步数 \(T\)、不同噪声水平 \(\sigma\)。

## 4. 资源与算力

- **文中未明确说明**：论文没有提及使用的GPU型号、数量、训练时长、内存消耗等具体硬件信息。作者可能使用了单块或少量GPU（如V100/A100）用于训练，但所有细节未披露。

## 5. 实验数量与充分性

- **实验组数**：论文提供了多个实验场景：
  1. **单噪声水平训练**：在固定\(\sigma\)下训练模型，比较去噪性能。
  2. **多噪声水平变形**：训练单个模型适应多个噪声水平（通过条件化输入或缩放交互矩阵）。
  3. **交互矩阵可视化**：展示学习到的\(\mathbf{J}\)的连接模式，与V1方向选择性图谱对比。
  4. **雅可比矩阵分解**：分析网络雅可比矩阵与交互矩阵的关系，揭示动力学机制。
  5. **潜变量分析**：发现部分潜变量与视觉输入断开连接，形成层次化表示（“沉默”神经元）。
  6. **泛化测试**：在未见过的噪声水平或图像类型上测试模型。
- **充分性与公平性**：
  - **充分性**：实验覆盖了去噪定量对比、生物结构定性对比、机制剖析（雅可比分解）以及泛化能力，较为全面。但缺少与其他最新扩散模型（如DDIM、score-SDE）的定量对比，也缺少在大型标准图像基准（如CIFAR-10/100）上的评估，使得在机器学习社区中的通用性存疑。
  - **公平性**：对比方法选择合理（因子化 vs 非因子化），消融实验明确。但标准扩散模型的具体实现细节（网络架构、训练配置）未完全对齐，可能引入差异。

## 6. 论文的主要结论与发现

- **学习到的交互矩阵类似V1水平连接**：\(\mathbf{J}\)的非零元素优先连接具有相似方向调谐的潜变量，形成局部抑制性、长程协作性的连接模式，与V1浅层水平连接的解剖生理数据高度吻合。
- **模型去噪性能接近黑盒扩散模型**：在自然图像去噪任务上，尽管模型结构极简（线性字典+线性递归），其表现与复杂UNet架构的扩散模型相当，甚至在某些极端噪声下恢复出更长的轮廓。
- **机制性可解释**：网络的雅可比矩阵可以分解为\(\mathbf{I} - \text{diag}(\mathbf{1}_{...}) \mathbf{J}\)，从而直接看出递归动力学如何引导潜变量沿数据流行方向移动。交互矩阵\(\mathbf{J}\)决定哪些特征组合被“提升”或“抑制”，形成连续变形族。
- **层次化表示的涌现**：训练后约20-30%的潜变量不再从图像输入接收有效信号（即\(\mathbf{a}_t\)接近零），它们仅通过\(\mathbf{J}\)与其他潜变量交互，形成“高阶”约束，强制全局一致性，类似于V1中的非经典感受野调制。
- **桥接神经科学与机器学习**：该模型为V1功能连接提供了具体可检验的假设（例如，特定连接模式对特定噪声水平的适应性），同时揭示了扩散模型内部通过交互矩阵实现流形学习的机制。

## 7. 优点

- **高度可解释性**：模型参数（字典\(\mathbf{W}\)、交互矩阵\(\mathbf{J}\)）均有直接生物意义，雅可比分解提供了动力学机制的可视化理解，这是标准扩散模型难以做到的。
- **简洁而有效**：仅使用线性操作和ReLU，训练效率高，却能达到接近复杂黑盒模型的性能，凸显了交互矩阵先验的重要性。
- **桥接作用突出**：首次将稀疏编码与扩散模型等价起来，用神经科学语言诠释了扩散模型的内部机制，对两个领域都有启发。
- **计算可扩展**：使用隐式微分避免了递归展开，训练代价较小，适合大规模场景（潜变量数量可较大）。
- **发现新的科学现象**：例如“断开输入连接的潜变量”这一层次化表示，可能揭示了V1中某种未受关注的细胞类型。

## 8. 不足与局限

- **实验覆盖范围有限**：
  - 仅使用小尺寸图像patch（如16×16），未在更大图像或完整数据集上测试，普适性未验证。
  - 对比的标准扩散模型可能采用了不同的训练目标（如ELBO），且未包含多个噪声水平的训练比较，无法完全说明等价性。
  - 未在自然图像以外的域（如纹理、人脸）评估，缺乏领域泛化证据。
- **噪声水平限制**：模型仅针对单一噪声水平训练（或有限几个），而真实扩散模型需要连续噪声表。论文虽提到了扩展方法，但未深入验证。这限制了其作为通用扩散模型的实用性。
- **训练细节缺失**：未提供训练超参数（字典大小\(\mathbf{m}\)、迭代步数\(T\)、学习率、批次大小等），影响可复现性。
- **生物验证不足**：虽然交互矩阵与V1连接结构定性相似，但缺乏定量对比（如连接密度分布、方向调谐图相关性数值），也没有提出可验证的神经电生理预测（例如预测特定细胞对噪声的反应）。
- **交互矩阵的对称性假设**：论文假设\(\mathbf{J}\)为对称矩阵，这虽然简化了分析，但V1中的突触连接在生物上未必对称（如前馈与反馈不同），可能限制了生物真实性。
- **消融实验不完整**：没有系统探究不同字典大小、不同稀疏正则化强度对交互矩阵结构和去噪性能的影响。

（完）
