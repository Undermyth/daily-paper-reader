---
title: "TF-Engram: A Train-Free Engram with SSD-Backed Memory for Large Language Models"
title_zh: "TF-Engram: 一种基于SSD内存的无训练脉冲记忆，用于大型语言模型"
authors: "Yutang Ma, Kecheng Huang, Xikun Jiang, Zili Shao"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.07388v1"
tags: ["query:la"]
score: 9.0
evidence: 离线构建短语级语义记忆作为联想记忆
tldr: 大语言模型通过参数隐式存储知识，但扩展知识需高昂的预训练或微调成本。现有Engram式内存虽能紧凑注入隐藏状态，但哈希压缩导致短语语义碰撞。TF-Engram离线构建短语级语义记忆表，利用GPU-DRAM-SSD三级存储，并通过早退引导预测预取隐藏外部内存延迟。在Qwen3-0.6B上，平均得分从57.6提升至59.4，优于冻结基线和LoRA，且SSD存储大幅降低GPU内存需求，吞吐损失被有效恢复。结果表明静态短语记忆可作为可扩展、免训练、低开销的系统组件集成到LLM推理中。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.07388v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1805, \"height\": 774, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07388v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 831, \"height\": 867, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07388v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 884, \"height\": 502, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07388v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 882, \"height\": 488, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07388v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 883, \"height\": 478, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.07388v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 883, \"height\": 401, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.07388v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 795, \"height\": 220, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07388v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 732, \"height\": 417, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.07388v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 957, \"height\": 510, \"label\": \"Table\"}]"
motivation: 大模型隐式存储知识导致知识扩展成本高；现有Engram内存因哈希压缩损害短语语义保真度。
method: 离线构建短语级语义记忆表，存储于GPU-DRAM-SSD层级，使用早退引导预测预取隐藏外部内存延迟。
result: 在Qwen3-0.6B上，平均得分从57.6升至59.4，超越冻结基线和LoRA，SSD存储大幅减少GPU内存占用。
conclusion: 静态短语内存可作为可扩展、免训练、低开销的系统组件集成到LLM推理中。
---

## 摘要
大型语言模型（LLM）将事实知识和领域特定模式隐式地存储在密集的Transformer参数中，使得通过预训练、微调、检索增强或更长的上下文进行知识扩展成本高昂。脉冲记忆（Engram）提供了一种紧凑的隐藏状态注入路径，但现有的GPU常驻设计通常依赖于基于哈希的压缩，导致不相关的短语在共享槽中发生冲突，削弱了短语级别的语义保真度。我们提出了TF-Engram，一种无训练的Engram系统，它从外部语料库离线构建特定短语的语义记忆，将大型记忆表存储在GPU-DRAM-SSD层次结构中，并使用早期退出引导的预测性预取来隐藏自回归解码期间的外部内存延迟。在Qwen3-0.6B上，TF-Engram将平均下游得分从57.6提升至59.4，优于冻结主干网络和参数匹配的LoRA基线。系统评估表明，大型TF-Engram表可以以适度的离线成本构建，基于SSD的存储显著降低了GPU内存需求，而预测性预取可以恢复由外部内存访问引起的大部分吞吐量损失。这些结果表明，静态短语记忆可以作为可扩展、无训练、低开销的系统组件集成到LLM推理中。

## Abstract
Large Language Models (LLMs) store factual knowledge and domain-specific patterns implicitly in dense Transformer parameters, making knowledge expansion costly through pretraining, fine-tuning, retrieval augmentation, or longer contexts. Engram-style memory offers a compact hidden-state injection pathway, but existing GPU-resident designs often rely on hash-based compression, causing unrelated phrases to collide in shared slots and weakening phrase-level semantic fidelity. We present TF-Engram, a train-free Engram system that constructs phrase-specific semantic memory offline from external corpora, stores large memory tables across a GPU--DRAM--SSD hierarchy, and uses Early-Exit Guided Predictive Prefetching to hide external-memory latency during autoregressive decoding. On Qwen3-0.6B, TF-Engram improves the average downstream score from 57.6 to 59.4, outperforming both the frozen backbone and a parameter-matched LoRA baseline. System evaluation shows that large TF-Engram tables can be built with moderate offline cost, SSD-backed storage substantially reduces GPU memory demand, and predictive prefetching recovers much of the throughput loss caused by external memory access. These results demonstrate that static phrase memory can be integrated into LLM inference as a scalable, train-free, and low-overhead system component.

---

## 论文详细总结（自动生成）

### 论文核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）将知识隐式存储在密集的 Transformer 参数中，导致知识扩展需通过昂贵的预训练、微调、检索增强或长上下文，且现有 Engram 式内存依赖哈希压缩，造成不相关的短语在共享槽中碰撞，削弱短语级别语义保真度。
- **整体含义**：提出 TF-Engram，一种**无训练、基于 SSD 支持**的静态短语记忆系统，旨在**解耦知识存储与模型计算**，通过离线构建短语级语义记忆表、GPU‑DRAM‑SSD 三级层次存储和早期退出引导的预测预取，使大规模外部短语记忆能高效集成到 LLM 推理中，提升下游性能同时保持较低系统开销。

### 论文提出的方法论

- **核心思想**：将 Engram 记忆从紧凑的哈希压缩 GPU 表转变为**存储管理的分级外部记忆系统**，保留短语级独立条目，利用 SSD 扩展容量，并通过中间层预测提前预取隐藏外部访问延迟。
- **关键技术细节**：
  1. **无训练语义记忆构建**：离线从语料库（如 FineWeb‑Edu、Wikipedia、C4、arXiv、PubMed）挖掘候选短语（n‑gram、命名实体等），用冻结的语义编码器（Qwen3‑Embedding）生成嵌入向量，构建 <token前缀→短语ID→存储地址> 两级索引，匹配时采用最长匹配+频率权重聚合。
  2. **SSD 支持的分级存储**：三层层次 `M = M_gpu ∪ M_dram ∪ M_ssd`。GPU 缓存热条目，DRAM 缓存温条目，SSD 存储全量表（每向量 1024 维 FP16）。查找时从快至慢逐级检查，SSD 异步读入，并利用页/块合并减少随机 I/O。
  3. **早期退出引导的预测预取**：在第 L‑r 层（靠近末尾）附加辅助预取头，输出近似下一 token 分布 `p_early(x_{t+1}) = softmax(W_pref h^{L-r}_t)`；取 top‑K token 通过索引映射到候选短语，在剩余层计算期间**异步预取** SSD/DRAM 条目到 GPU。优先级评分 `s_i = p_i · r_i · c_i` 结合概率、效用、存储层级获益。
- **公式/算法流程**（文字说明）：
  - 记忆注入：融合隐藏态 `h'_t = h_t + g_t · m_t`（`m_t` 为加权聚合的检索向量，`g_t` 为门控）。
  - 预取窗口：`T_hide ≈ T_compute(层 L‑r+1 至 L)`，利用剩余层计算隐藏 SSD 延迟。
  - 最终输出仍由原始 LM 头产生，预取仅用于调度。

### 实验设计

- **数据集/场景**：离线构建语料包含 FineWeb‑Edu、Wikipedia、C4 English、arXiv、PubMed 的混合；下游评估 10 个基准：MMLU、ARC‑Challenge、HellaSwag、PIQA、WinoGrande、BoolQ、OpenBookQA、SciQ、TruthfulQA‑MC2、LAMBADA。
- **基准对比**：
  - **基线**：原始 Qwen3‑0.6B（冻结骨干）。
  - **对比方法**：参数匹配的 LoRA 微调（可训练参数数量近似于 TF‑Engram 的记忆表规模？实际对比表 3 中 LoRA 性能 58.7）。
- **实验硬件**（见资源部分）。
- **实验分类**：端到端效果、系统开销（离线构建时间/ GPU内存/吞吐/延迟）、敏感性分析（记忆规模 1M‑100M、早退层位置、Top‑K 预取预算）、消融实验（无预取、最终头引导预取 vs. 早退引导）。

### 资源与算力

- **硬件配置**（表 2）：
  - CPU：Intel Core i9‑14900K，24 核/32 线程，最大 6.0 GHz
  - GPU：NVIDIA GeForce RTX 5090，32 GB 显存
  - 主机内存：64 GB
  - SSD 存储：516 GB（NVMe）
- **训练/构建资源**：
  - 文中强调**无训练**（backbone 冻结），仅离线构建记忆表。
  - 离线构建时间：100M 条目约需 2.0 小时（其中 n‑gram 提取占 1.612h，嵌入生成 0.231h，索引构建 0.157h）。
  - 论文未说明使用多 GPU 或分布式训练，仅在单节点、单 GPU 上完成实验。
  - 预取头可保持冻结或进行轻量校准，但文中未明确校准所需资源（推测无需额外大规模训练）。

### 实验数量与充分性

- **实验组数**：端到端 1 组（3 方法 × 10 基准），系统开销 3 大组（构建、运行时、延迟），敏感性 2 参数（规模、层位置）+ 部分 Top‑K 效果，消融 3 变体。合计约 10+ 组数值结果，覆盖质量、效率、权衡。
- **充分性评价**：
  - **优点**：基准覆盖常识、推理、知识密集型任务；系统开销分析具体（时间、内存、吞吐、延迟）；消融验证各组件贡献；敏感性展示 trade‑off。
  - **不足**：仅评估 0.6B 规模，未在更大 LLM（如 7B/13B）上验证；未与 RAG、传统 Engram 等主流记忆增强方法直接对比；LoRA 虽参数匹配但可训练（TF‑Engram 无训练），两者公平性可商榷；部分基准（HellaSwag、WinoGrande）性能下降，说明非所有场景受益，但论文未深入分析原因。

### 论文的主要结论与发现

- TF‑Engram 在 Qwen3‑0.6B 上平均得分从 57.6 提升至 59.4（+1.8），优于参数匹配 LoRA（58.7），并在 8/10 基准上优于原始骨干。
- SSD 分级结构显著降低 GPU 内存需求（100M 条目仅需 3.06 GB GPU 内存，而 GPU 常驻 10M 表需 24.03 GB）。
- 预测预取恢复大部分吞吐损失：无预取 439.2 tok/s → Top‑128 预取 462.4 tok/s（基线 481.2 tok/s），解码延迟从 2.28 ms/tok 降至 2.16 ms/tok。
- 100M 条目表离线构建约 2 小时，主要瓶颈在 n‑gram 提取。
- 早退层选择（如第 8 层）和较大记忆规模提升效果，但带来适度系统开销。

### 优点

1. **无训练设计**：离线构建，不修改 backbone，可跨模型复用，适配新领域仅需重新提取+编码。
2. **SSD 分级存储**：突破 GPU 显存瓶颈，支持十亿级短语表；保留短语独立语义，避免哈希碰撞。
3. **早期退出预取**：巧妙利用中间层预测提前发起 I/O 与剩余 Transformer 计算重叠，有效隐藏 SSD 延迟。
4. **端到端系统整合**：从离线构建到在线推理形成完整方案，评估覆盖质量、开销、敏感性、消融，说服力强。

### 不足与局限

1. **规模验证不足**：仅在 0.6B 模型上测试，该规模下性能增益可能与超大模型不同（超大模型可能已有更多隐式知识）。
2. **对比缺失**：未与 RAG、传统 Engram（如 Memory Grafting）等同类方法比较，仅对比 LoRA，缺乏横向参照。
3. **部分任务退化**：HellaSwag、WinoGrande 上性能下降，说明静态短语记忆可能干扰上下文理解任务，论文未给出合理解释或补救措施。
4. **可训练性模糊**：早期退出头文中称可“train‑free 或 lightly calibrated”，实验未明确其训练方式，若需校准则与“无训练”宣称有出入。
5. **预取浪费风险**：Top‑K 预取可能引发无效 I/O（预测错误时），系统通过优先级评分缓解，但无定量分析预测准确性或浪费比例。
6. **硬件依赖**：实验基于高端消费级 GPU（RTX 5090）和 NVMe SSD，低端设备或大并发场景下预取窗口可能不足。

（完）
