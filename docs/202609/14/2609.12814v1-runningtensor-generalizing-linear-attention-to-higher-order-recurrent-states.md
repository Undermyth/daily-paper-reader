---
title: "RunningTensor: Generalizing Linear Attention to Higher-Order Recurrent States"
title_zh: RunningTensor：将线性注意力推广到高阶循环状态
authors: "Luca Herranz-Celotti, Vincent Guigue"
date: 2026-09-11
pdf: "https://arxiv.org/pdf/2609.12814v1"
tags: ["query:la"]
score: 9.0
evidence: 将线性注意力推广到高阶循环张量状态
tldr: 线性注意力和状态空间模型虽能线性时间建模，但其循环记忆仅是二阶张量（矩阵），限制了状态可表达的交互阶数。RunningTensor 将记忆推广为 o 阶张量，用秩1外积更新，并与 o-1 个向量查询收缩读取；二阶退化为线性注意力，三阶作为验证。该方法保持对序列长度 T 线性，并将工作记忆容量从 O(W^2) 提升至 O(W^o)。在合成多查询联想召回及预训练后的语言理解与检索任务上，均优于线性注意力和 SSM 基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.12814v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 571, \"height\": 342, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.12814v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1229, \"height\": 323, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.12814v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1362, \"height\": 493, \"label\": \"Table\"}]"
motivation: 现有线性注意力与SSM的循环记忆固定为矩阵，容量和可表示交互阶数受限，难以应对多查询联想召回等需要更高阶状态的任务。
method: 提出RunningTensor，将循环状态推广为o阶张量，以秩1外积更新，并用o-1个查询向量收缩读取，同时保留循环与并行形式且保持线性复杂度。
result: 在合成多查询联想召回上超越线性注意力与SSM基线；预训练后，在语言理解和非合成检索任务上也取得提升，工作记忆容量由O(W^2)增至O(W^o)。
conclusion: 结果表明，高阶循环状态能为序列模型提供矩阵状态之外的有效额外记忆容量，为推广线性注意力与状态空间模型提供了新方向。
---

## 摘要
线性注意力和状态空间模型提供了线性时间的序列建模，但它们的循环记忆仍是一个二阶张量（矩阵），限制了状态中可表示的交互阶数。我们提出了 RunningTensor，它将这种记忆推广为 o 阶张量，通过秩-1 外积进行更新，并通过与 o-1 个向量查询缩并来读取。2 阶可恢复线性注意力；我们以 3 阶作为概念验证进行研究，同时保留循环形式和并行形式，且在序列长度 T 上保持线性，并将工作记忆容量从 O(W^2) 提升到 O(W^o)。在合成多查询关联召回任务上，RunningTensor 优于线性注意力和 SSM 基线。预训练后，它还在语言理解和非合成检索任务上提升了性能，表明高阶循环状态能够提供超越矩阵值状态的有用额外记忆容量。

## Abstract
Linear attention and state-space models provide linear-time sequence modeling, but their recurrent memory remains a second-order tensor (a matrix), limiting the order of interactions that can be represented in the state. We introduce the RunningTensor, which generalizes this memory to an order-$o$ tensor, updated by a rank-1 outer product and read by contracting against $o-1$ vector queries. Order $2$ recovers linear attention; we study order $3$ as a proof of concept, retaining both recurrent and parallel forms while remaining linear in sequence length $T$ and improving working memory capacity from $\mathcal{O}(W^2)$ to $\mathcal{O}(W^o)$. On synthetic multi-query associative recall, RunningTensor outperforms linear-attention and SSM baselines. After pretraining, it also improves performance on language-understanding and non-synthetic retrieval tasks, suggesting that higher-order recurrent state can provide useful additional memory capacity beyond matrix-valued state.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **背景**：Transformer 自注意力需要物化 \(O(T^2)\) 的成对分数矩阵，长序列计算和内存代价高。线性注意力与 SSM（如 S4、Mamba、Gated DeltaNet、Longhorn）将检索改写为线性循环，在推理时对序列长度 \(T\) 只需 \(O(1)\)，训练时也可并行化。
- **核心问题**：这些模型的循环记忆本质上仍是二阶张量，即矩阵状态 \(S_t \in \mathbb{R}^{d_v \times d_k}\)，容量为 \(O(W^2)\)，读取时只用一个查询向量收缩。这限制了状态可表示的交互阶数和记忆容量。
- **论文主张**：能否在不放弃对 \(T\) 线性成本的前提下，把循环状态从矩阵提升为高阶张量？作者提出 **RunningTensor**，将状态推广为 \(o\) 阶张量，用秩-1 外积更新，并与 \(o-1\) 个查询向量收缩读取。二阶退化为线性注意力，三阶作为概念验证。整体意义在于：为线性时间序列模型提供超越矩阵状态的额外记忆容量。

## 2. 方法论

- **核心思想**：用高阶张量作为循环记忆。一般形式为：
  - 状态：\(S_t = S_{t-1} + v_t \otimes k_t \otimes r_t \in \mathbb{R}^{d_v \times d_k \times d_r}\)
  - 读取：\(o_t = \langle S_t, q_t, u_t \rangle\)，即用两个查询向量收缩三阶张量的后两维。
  - 对一般阶数 \(o\)：累积 \(o\) 个向量的秩-1 外积，并用 \(o-1\) 个查询向量收缩读取。
- **三阶实例**：
  - 元素形式：\((o_t)_a = \sum_{b=1}^{d_k}\sum_{c=1}^{d_r} (S_t)_{abc}(q_t)_b(u_t)_c\)。
  - 展开因果求和后：
    \[
    o_t = \sum_{i=1}^t v_i (k_i^\top q_t)(r_i^\top u_t)
    \]
  - 并行矩阵形式：
    \[
    O = \big((QK^\top)\odot(UR^\top)\odot M\big)V
    \]
    其中 \(M\) 是因果掩码，\(\odot\) 为逐元素乘。
- **理论包含性**：命题 1 指出，当 \(d_r=1\) 且 \(u_t=r_t=1\) 时，RunningTensor 退化为因果线性
