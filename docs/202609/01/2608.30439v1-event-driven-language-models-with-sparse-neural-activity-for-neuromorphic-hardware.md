---
title: Event-Driven Language Models with Sparse Neural Activity for Neuromorphic Hardware
title_zh: 基于稀疏神经活动的事件驱动语言模型用于神经形态硬件
authors: "Simon Richter, Ruhai Lin, Jason Yik, Taylor Kergan, Rui-Jie Zhu, Farshad Moradi, Jason Eshraghian"
date: 2026-08-31
pdf: "https://arxiv.org/pdf/2608.30439v1"
tags: ["query:la"]
score: 9.0
evidence: 直接面向线性注意力/状态空间模型的稀疏激活与高效序列处理
tldr: 大语言模型推理瓶颈在于KV缓存和注意力二次复杂度；状态空间模型虽采用线性注意力，但稠密投影仍耗资源。本文在重度量化线性注意力模型中引入可训练阈值，将低于阈值的激活置零，同时保留异常值，实现近似无损的性能，减少最多4倍有效计算。在事件驱动神经形态芯片上，非结构化稀疏性可直接提升计算与通信效率，预计相对边缘GPU提升37倍吞吐、降低16倍功耗，并优于非稀疏基线。因此，稀疏量化线性注意力模型适合部署在事件驱动多核平台。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.30439v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 310, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30439v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1541, \"height\": 554, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30439v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 901, \"height\": 463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30439v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1584, \"height\": 553, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.30439v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 915, \"height\": 487, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30439v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 922, \"height\": 452, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.30439v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1387, \"height\": 642, \"label\": \"Table\"}]"
motivation: Transformer推理受KV缓存和注意力开销制约，状态空间模型的稠密投影即使量化也计算昂贵。
method: 在重度量化线性注意力模型中引入可训练阈值，将低于±Δ的激活置零，同时保留异常值以维持性能。
result: 在近无损下减少最多4倍有效计算，预计比边缘GPU提升37倍吞吐、降低16倍功耗，并优于非稀疏基线。
conclusion: 该工作表明，稀疏量化线性注意力模型是在事件驱动多核芯片上高效部署大语言模型的自然方案。
---

## 摘要
基于Transformer的大型语言模型（LLM）推理通常受限于内存受限的KV缓存和二次注意力开销。状态空间模型（SSM）通过线性注意力和固定大小的循环状态缓解了这一问题，但其大型密集线性投影即使在量化后仍计算成本高昂。我们提出一种方法，在重度量化的线性注意力模型中诱导稀疏神经活动，且性能损失极小。低于每个投影可训练阈值（$\pm Δ$）的激活被置零，同时保留关键异常值，实现了与密集模型相当的性能，而有效算术运算量最多减少4倍。针对多核、多芯片神经形态平台（其中事件驱动执行将非结构化稀疏性转化为计算和通信层面的吞吐量，这是GPU架构根本缺乏的能力），我们预计与同等Transformer模型在边缘GPU上的推理相比，吞吐量最高提升37倍，功耗降低16倍，相比非稀疏化基线最高提升5.4倍。这些结果将稀疏、量化的线性注意力模型定位为在事件驱动多核平台上部署LLM的自然选择。

## Abstract
Inference with transformer-based large language models (LLMs) is often limited by the memory-bound KV cache and quadratic attention cost. State-space models (SSMs) mitigate this through linear attention and fixed-size recurrent states, but their large dense linear projections remain computationally expensive even after quantization. We introduce a method that induces sparse neural activity in heavily quantized linear-attention models with minimal performance loss. Activations below a per-projection trainable threshold ($\pm Δ$) are nullified while preserving crucial outliers, achieving comparable performance to dense models with up to 4$\times$ fewer effective arithmetic operations. Targeting a multi-core, multi-chip neuromorphic platform, where event-driven execution converts unstructured sparsity into throughput at both the compute and communication levels, a capability GPU architectures fundamentally lack, we project up to 37$\times$ higher throughput and 16$\times$ lower power versus edge GPU inference of a comparable transformer-based model, and up to 5.4$\times$ improvements over the non-sparsified baseline. These results position sparse, quantized linear-attention models as a natural fit for deploying LLMs on event-driven multi-core platforms.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义

- **研究背景**：基于Transformer的大语言模型（LLM）推理受限于两个主要瓶颈：内存受限的KV缓存和随序列长度二次增长的注意力计算开销。状态空间模型（SSM）和线性注意力模型通过线性注意力机制和固定大小的循环状态缓解了这些问题，但其大型密集线性投影即使在量化后仍然计算成本高昂，阻碍了在边缘设备上的部署。
- **核心问题**：如何在不显著损失性能的前提下，在重度量化的线性注意力模型中诱导出高水平的激活稀疏性，并利用神经形态硬件的事件驱动特性来实现高效的LLM推理。
- **整体意义**：该工作表明，稀疏量化线性注意力模型是在事件驱动多核芯片上高效部署大语言模型的自然方案。它将稀疏性转化为实际的计算和通信效率提升，这是传统GPU架构本质上无法实现的。

## 2. 方法论：核心思想与技术细节

### 核心思想

在重度量化的线性注意力模型（MMFreeLM，基于三元权重和8位定点激活）中，通过可学习的阈值参数在每个线性投影的输入端诱导激活稀疏性。零值激活可以在矩阵-向量乘法（MVM）中跳过相应权重行的内存访问和计算。

### 关键技术细节

- **稀疏预激活函数**（公式1）：
  \[
  x_{\text{sparse}} = \text{sign}(x) \cdot \text{ReLU}(|x| - \Delta)
  \]
  - 采用双侧ReLU形式，将落在±Δ范围内的激活置零
  - 同时保留正负方向的**高幅值异常值**（outliers），这对LLM性能至关重要
  - Δ为可学习参数，对每个投影独立优化

- **平滑梯度代理**（公式2）：
  \[
  \frac{dx_{\text{sparse}}}{dx_{\text{smooth}}} = \sigma(C(|x| - \Delta))
  \]
  - 使用Sigmoid函数作为反向传播的代理梯度，避免ReLU导致的大量"死亡神经元"问题
  - 斜率参数C=20，固定不变

- **可微分的L0稀疏性代理损失**（公式3）：
  \[
  L = L_{\text{task}} + \lambda (1 - \hat{s}), \quad \hat{s} = \frac{1}{N} \sum_{i=1}^{N} \exp(-k |x_{q\text{-sparse},i}|)
  \]
  - λ控制稀疏性惩罚强度，k=10为指数陡度参数
  - 惩罚按层加权，考虑各层零激活带来的MAC操作减少量
  - 惩罚项采用线性预热（前5%训练步），避免训练初期过度稀疏

### 性能建模

- **有效MAC密度r**：各投影激活密度的尺寸加权平均值，公式(7)
- **层内加速因子**：\(S_{\text{MAC}}(r) = 1/r\)
- **层间通信加速因子**：\(S_{\text{comm}}(\rho_{\text{com}}) = 1/(0.8 + 0.2\rho_{\text{com}})\)，利用事件驱动链路跳过零激活包
- **预填充阶段**（流水线模式）：吞吐量受最慢块限制，使用最大MAC密度r_max
- **生成阶段**（直通模式）：延迟各块线性累加，使用平均MAC密度r_avg

## 3. 实验设计

- **模型与数据集**：在预训练的370M和2.7B MMFreeLM模型上，使用FineWebEdu数据集继续训练4B token。
- **对比方法**：
  - ReLU化（ReLU-fication）基线
  - dReLU方法（TurboSparse）
  - 继续训练基线（控制额外训练数据的效果）
  - 提出的方法在两种稀疏性惩罚强度（λ=1.0和λ=2.0）下的变体
- **基准测试**：零样本任务评估，包括ARC-Easy、ARC-Challenge、HellaSwag、OpenBookQA、PIQA、WinoGrande。
- **硬件对比**：NVIDIA H100 GPU、Intel Loihi 2、NVIDIA Jetson边缘GPU，对比模型包括Qwen2 (500M)和Alireo (400M)。
- **敏感性分析（消融/预研究）**：通过强制top-k稀疏性，分析不同投影类型（FFN各投影、MLGRU各投影、LM Head）和不同层（共24层）对稀疏性的敏感度。

## 4. 资源与算力

- **文中明确提到的信息**：
  - 继续训练使用了4B tokens的FineWebEdu数据集
  - 在Loihi 2平台上的多芯片部署（24芯片）参考了文献[26]的实测数据
- **未明确说明的信息**：论文**未明确说明**训练所使用的GPU型号、数量、训练时长或总计算量。仅在对比实验的表格中引用了H100和Jetson上推理的指标（来自其他文献的部署结果），但训练自身的硬件资源未交代。这是信息完整度上的一个小缺失。

## 5. 实验数量与充分性

- **实验数量**：
  - 敏感性分析实验（投影级和层级的强制稀疏性）
  - 2个模型规模（370M、2.7B）× 2种λ值 + 3个基线对比
  - 6项零样本基准任务
  - 2种推断模式（预填充、生成）× 4种序列长度（500、1000、4000、8000）
  - 与其他Transformer模型的跨硬件对比
- **充分性与公平性**：
  - **优点**：对比方法选择合理（ReLU、dReLU为业界主流方法）；同时提供了基线继续训练结果以控制额外数据的影响；敏感性分析设计严谨，为方法设计提供了依据；多头硬件对比较为全面。
  - **局限**：仅在一个数据集（FineWebEdu）上微调评估；0样本评估任务数量有限（仅6个）；LM Head在部署实现中被排除，导致部分指标计算口径不一致；对Loihi 2的性能提升是基于模型的**外推预测**，而非实际芯片测量。

## 6. 主要结论与发现

1. **方法与基线对比**：
   - 提出的方法（λ=1）在370M模型上实现69.5%（含LM Head为64.3%）的有效MAC稀疏率，是dReLU基线（25.1%）的近3倍
   - 370M（λ=1）平均推理得分为41.24，优于dReLU (40.25)，而有效MAC仅为dReLU的约45%（118M vs 263M）
   - λ=2的370M模型达到76.2%的MAC稀疏率，性能损失仅约2个百分点
2. **敏感性分析发现**：
   - 状态转换相关投影（I、F）对稀疏性敏感，而FFN的Down投影对稀疏性高度耐受
   - 首层、末层和中间层对稀疏性更敏感，与部分Transformer研究一致
3. **硬件效率预测**：
   - 相较于Loihi 2上的密集MMFreeLM基线：生成阶段提升5.4倍，预填充阶段提升3.5倍（吞吐量和能效）
   - 相较于Jetson边缘GPU上的Transformer模型：吞吐量最高提升37倍（生成阶段）
   - 相较于Loihi 2上的Transformer：能效提升16倍

## 7. 优点

- **方法设计精巧**：以极简的方式（一个可学习的双侧门控函数）实现了模型级激活稀疏性，同时尊重了异常值的重要性；平滑反向代理梯度解决了死神经元问题；L0代理损失提供了端到端的稀疏率控制。
- **关注实际部署**：考虑了神经形态硬件的实际执行特性（事件驱动，零跳转、通信量正比于激活量），构建了合理的性能模型。
- **敏感性分析扎实**：投影级和层级两个维度的敏感性分析是理解任务中何处有效率提升空间的关键，为方法设计提供了实证支撑。
- **结果显著且清晰**：在较低性能损耗下实现了大幅度的计算量减少，且优于既有方法。

## 8. 不足与局限

- **稀疏性分布不均衡**：虽然模型整体MAC稀疏率很高，但LM Head区的稀疏率较低（24.0%/40.2%），且LM Head并未包含在Loihi 2的实际部署中。在某种程度上有夸大端到端效果的倾向。
- **对Loihi 2的收益是预测而非实测**：论文中的5.4×、37×等关键数字均通过性能模型外推得出，并非在芯片上实际测量的结果（尽管基线的部署数据是真实的）。因此实际的稀疏硬件收益仍存不确定性。
- **任务的广泛性受限**：仅验证了6个英语零样本常识推理任务，未覆盖代码、数学、多语言、对话等主流LLM能力维度，整体评估的全面性受限。
- **方法泛化性待验证**：方法只在MMFreeLM这类极端量化的三元权重模型上验证，对其他主流线性注意力模型（如Mamba、Griffin等）的适应性以及不同量化粒度下（如4bit、8bit权重）的效果尚未说明，普适性有待进一步检验。
- **对GPU的适配性有偏颇**：作者反复强调GPU很难从非结构化稀疏性中获益，因此只给出了Loihi 2上预测的收益；对于GPU上的稀疏推理能否获得实际性能提升（哪怕是降低的）则避而不谈，需要看到GPU侧的对照试验才能更全面的评估这种方法的适用边界。

（完）
