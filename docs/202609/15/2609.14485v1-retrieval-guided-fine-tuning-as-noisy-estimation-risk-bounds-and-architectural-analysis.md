---
title: "Retrieval-Guided Fine-Tuning as Noisy Estimation: Risk bounds and Architectural Analysis"
title_zh: 检索引导微调作为含噪估计：风险界与架构分析
authors: "Bhargav Lad, Yifan Hao"
date: 2026-09-13
pdf: "https://arxiv.org/pdf/2609.14485v1"
tags: ["query:la"]
score: 6.0
evidence: 用OLS代理单层线性自注意力并进行检索分析
tldr: "检索引导微调（RAG-FT）将检索数据纳入训练目标，但检索噪声的统计影响尚不清晰。该工作将其建模为多任务线性回归估计，以 OLS 代理单层线性自注意力，给出有限样本风险界。同方差噪声下，检索失败随任务可分性相对噪声指数衰减，并给出 RAG-FT 优于仅目标与全语料训练的条件；在 DPN 下证明注意力估计器偏差发散为 Θ(n^{2q})，而 OLS 风险保持 Θ(d/n)。结论：不稳定源于线性自注意力固定无权聚合，按可靠性重加权可消除。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.14485v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1378, \"height\": 518, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.14485v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 542, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.14485v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 858, \"height\": 343, \"label\": \"Table\"}]"
motivation: RAG-FT 将检索数据直接纳入训练目标，但训练时检索噪声的统计后果缺乏理论刻画。
method: 将 RAG-FT 视为多任务线性回归估计，以 OLS 代理单层线性自注意力，推导有限样本风险界，并引入距离比例噪声（DPN）比较估计器。
result: "同方差噪声下检索失败随任务可分性指数衰减；DPN 下注意力估计器偏差发散为 Θ(n^{2q})，OLS 风险仍为 Θ(d/n)。"
conclusion: 不稳定性源于线性自注意力固定无权聚合，而非噪声检索本身；按可靠性重加权可消除，仿真验证速率分离。
---

## 摘要
检索引导微调（RAG-FT）将检索到的数据直接纳入训练目标，但训练过程中含噪检索的统计后果在理论上仍缺乏充分刻画。我们通过将 RAG-FT 建模为多任务线性回归框架中的估计问题来研究该问题，并使用普通最小二乘（OLS）代理表示单层线性自注意力，以获得有限样本风险界。在同方差检索噪声下，我们表明检索失败随任务分离度相对于噪声呈指数衰减，并推导出显式的有限样本条件，在这些条件下 RAG-FT 比仅目标训练和全语料训练都取得更低风险。然后，我们引入距离成比例噪声（DPN）模型，其中检索质量随排序位置而下降，并在相同检索过程下比较两个估计器：OLS 代理与线性自注意力的字面、均匀权重前向传播。我们证明，即使在精确检索下，注意力估计器的偏差也会以 $Θ(n^{2q})$ 发散，而对于每个噪声指数 $q>0$，OLS 风险仍保持为 $Θ(d/n)$。这些结果表明，不稳定性并不在于含噪检索本身，而在于字面 LSA 前向传播的固定、未加权聚合；通过可靠性重新加权可在经验上消除这种不稳定性。我们通过对 DPN 模型的直接模拟验证了所预测的速率分离。

## Abstract
Retrieval-Guided Fine-Tuning (RAG-FT) incorporates retrieved data directly into the training objective, but the statistical consequences of noisy retrieval during training remain theoretically undercharacterized. We study this question by modeling RAG-FT as an estimation problem in a multi-task linear regression framework, using an OLS proxy for single-layer linear self-attention to obtain finite-sample risk bounds. Under homoscedastic retrieval noise, we show that retrieval failure decays exponentially with task separation relative to noise, and derive explicit finite-sample conditions under which RAG-FT achieves lower risk than both target-only and full-corpus training. We then introduce a Distance-Proportional Noise (DPN) model, in which retrieval quality degrades with rank, and compare two estimators under the same retrieval process: the OLS proxy and the literal, uniform-weight forward pass of linear self-attention. We prove that the attention estimator's bias diverges as $Θ(n^{2q})$ even under exact retrieval, while OLS risk remains $Θ(d/n)$ for every noise exponent $q>0$. These results locate the instability not in noisy retrieval itself, but in the fixed, unweighted aggregation of the literal LSA forward pass, which reweighting by reliability empirically removes. We validate the predicted rate separation through direct simulation of the DPN model.