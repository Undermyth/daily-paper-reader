---
title: Persistent Recurrent Memory Between Transformer Layers - Improves Language Model Generalization
title_zh: Transformer 层间的持久循环记忆——提升语言模型泛化能力
authors: Eduardo Novaes Hering
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.17251v1"
tags: ["query:la"]
score: 8.0
evidence: Transformer层间的持久循环记忆模块，含交叉注意力与GRU
tldr: "小规模decoder-only语言模型泛化不足。本文在6层Transformer上下半层间加入持久循环记忆：用cross-attention读取隐状态，GRU更新，再以门控加法调制后续层。仅增3.7%参数，留出集损失从2.438降至1.743（降28.5%），5个种子显著且泛化差距由0.26降至0.12。消融显示增益源于记忆拓扑而非辅助自预测，探针显示其编码叙事位置更高效。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.17251v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 979, \"height\": 1266, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.17251v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.17251v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1165, \"height\": 362, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.17251v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1123, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.17251v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1542, \"height\": 268, \"label\": \"Table\"}]"
motivation: 小规模Transformer语言模型易过拟合、泛化不足，标准层间信息流缺少跨层持久记忆，难以高效保留叙事位置等上下文信息。
method: 在6层Transformer中间加入持久循环记忆，通过cross-attention读取隐状态，用GRU更新，再门控调制上层。
result: "留出集损失从2.438降至1.743（降28.5%），5个种子显著，泛化差距0.26→0.12，叙事位置探针52%高于33%。"
conclusion: 消融证实增益源于持久记忆拓扑而非辅助自预测目标；轻量循环记忆桥接Transformer层可有效提升小模型泛化。
---

## 摘要
我们提出一种对仅解码器 Transformer 的简单架构修改：一个持久循环状态，它通过交叉注意力观察隐藏表示，通过 GRU 更新自身，并通过门控加法调制后续处理。将该模块插入 6 层 Transformer 的下半部分与上半部分之间，仅增加 3.7% 的额外参数，同时将评估损失从 2.438 ± 0.004 降至 1.743 ± 0.018，相当于在留出语言建模数据上降低 28.5%。该改进在 5 个随机种子下具有统计显著性（p < 0.01），并且对应于过拟合减轻（泛化差距 0.12 对 0.26）。通过受控消融，我们证明该改进完全来自持久记忆拓扑，而非辅助自预测目标。具有相同拓扑但没有辅助损失的模型性能相当，而随机辅助损失没有带来任何收益。表示探测表明，持久状态编码了叙事位置（52% 对 33% 的随机水平）——这是标准注意力维持效率较低的信息。我们的结果表明，用轻量级循环记忆连接 Transformer 层是一种简单、有效的方法，可提升小规模语言模型的泛化能力。

## Abstract
We introduce a simple architectural modification to decoder-only transformers: a persistent recurrent state that observes hidden representations via cross-attention, updates itself through a GRU, and modulates subsequent processing via gated addition. Inserted between the lower and upper halves of a 6-layer transformer, this module adds only 3.7\% additional parameters while reducing evaluation loss from $2.438 \pm 0.004$ to $1.743 \pm 0.018$, corresponding to a 28.5\% reduction on held-out language modeling data. The improvement is statistically significant across 5 random seeds ($p < 0.01$) and corresponds to reduced overfitting (generalization gap 0.12 vs 0.26). Through controlled ablations, we demonstrate that the improvement stems entirely from the persistent memory topology, not from auxiliary self-prediction objectives. A model with identical topology but no auxiliary loss performs equivalently, while a random auxiliary loss provides no benefit. Representation probing reveals that the persistent state encodes narrative position (52\% vs 33\% chance level)---information that standard attention maintains less efficiently. Our results suggest that bridging transformer layers with a lightweight recurrent memory is a simple, effective approach to improving generalization in small-scale language models.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究背景**：标准 decoder-only Transformer 仅通过逐层演化的隐藏表示传递信息，每层都需依赖注意力从完整序列中重新推导上下文，缺少显式、稳定、压缩的跨层信息通路。
- **核心问题**：小规模语言模型容易过拟合、泛化不足；注意力机制虽有效，但未必能高效维持“叙事位置”等缓慢变化的高层上下文。
- **整体含义**：论文探索在 Transformer 层间插入轻量级持久循环状态，形成“观察→更新→影响”的信息桥接，以提升小模型泛化能力。
- **主要贡献**：不仅报告性能提升，还通过受控消融证明增益来自“持久记忆拓扑”本身，而非辅助自预测目标。

## 2. 方法论

- **核心思想**：在 6 层 Transformer 的第 3 层后，即下半层与上半层之间，插入 Persistent Recurrent Memory（PRM）模块。该模块维护一个跨前向传播持久存在的状态向量 \(s \in \mathbb{R}^d\)。
- **三步操作**：
  - **Observe / 观察**：持久状态通过多头交叉注意力读取下层隐藏状态 \(H\)：
    \[
    o = \text{CrossAttention}(Q=s, K=H, V=H)
    \]
  - **Update / 更新**：用 GRU 更新状态：
    \[
    s' = \text{GRU}(input=o, hidden=s)
    \]
    递归执行 \(L=2\) 次，每步后做 Layer Normalization。
  - **Influence / 影响**：通过门控加法调制上层隐藏表示：
    \[
    g = \sigma(W_g[H; s'])
    \]
    \[
    H' = H + g \odot (W_p \cdot s')
    \]
    其中 \(g\) 学习何时让持久状态影响后续处理。
- **训练方式**：仅使用标准 next-token prediction 交叉熵损失，AdamW 优化，dropout 0.1；无需辅助目标。
- **通用性说明**：作者强调“observe→update→influence”是通用架构原语，GRU 只是其中一种实例，可替换为 LSTM、状态空间模型或线性循环算子。

## 3. 实验设计

- **数据集**：TinyStories，选取 15,000 个故事，GPT-2 tokenizer，词表 50,257，切成固定长度 64 token 序列，按 85/15 划分训练/评估，训练集约 44,000 条序列，随机种子下重新划分。
- **模型配置**：\(d_{model}=192\)，\(n_{heads}=6\)，\(n_{layers}=6\)，\(max\_seq\_len=128\)，dropout 0.1；AdamW，学习率 \(5\times10^{-4}\)，weight decay 0.01，15 epochs，batch size 32，梯度裁剪 1.0，随机种子 42–46。
- **评估指标**：
  - 留出集交叉熵损失，即 eval loss；
  - 泛化差距：eval loss − train loss；
  - 线性探针：主题、叙事位置、不确定性检测。
- **对比方法**：
  - **PRM**：完整持久循环记忆；
  - **Standard**：标准 Transformer；
  - **GRU Memory**：与 PRM 同拓扑，但去掉自预测辅助目标；
  - **Random Aux**：标准 Transformer 加随机辅助损失，用于检验“任意辅助损失是否有益”。

## 4. 资源与算力

- 论文**未明确说明** GPU 型号、GPU 数量、训练总时长或总 GPU 小时数。
- 仅致谢中提到，作者所在 FITec Labs / Ericsson São Paulo 的计算基础设施间接支持了多随机种子验证实验。
- 可复现信息主要是训练超参数：AdamW、15 epochs、batch size 32、5 个随机种子等；但算力规模不可核验。

## 5. 实验数量与充分性

- **主实验**：4 个模型变体 × 5 个随机种子，报告均值与 95% 置信区间。
- **消融实验**：围绕三个假设展开：
  - H1：持久状态拓扑是否足够；
  - H2：自预测是否额外有益；
  - H3：任意辅助损失是否有益。
- **成对比较**：PRM vs Standard、GRU Memory vs Standard、PRM vs GRU Memory、Random Aux vs Standard。
- **表示探针**：对 4 个模型分别探测主题、叙事位置、不确定性三类属性。
- **充分性评价**：
  - 优点：5 个种子、95% CI、\(p<0.01\)，并设置 Random Aux 与 GRU Memory 对照，消融较严谨。
  - 局限：仅一个数据集、一个 22M 规模模型；未系统研究 PRM 插入位置、循环次数、循环算子、层数变化；未与“增加同等参数量或层宽”的标准模型严格对比。
  - 公平性：各模型超参数相同，参数量接近（PRM 22.8M，Standard 22.0M），但 PRM 多约 816K 参数，严格同等参数预算下仍需补充验证。

## 6. 主要结论与发现

- **性能提升**：PRM 将 eval loss 从 \(2.438 \pm 0.004\) 降至 \(1.743 \pm 0.018\)，降低约 **28.5%**；泛化差距从 \(0.255 \pm 0.003\) 降至 \(0.122 \pm 0.008\)。
- **参数效率**：PRM 仅增加 816K 参数，约 **3.7%** 开销；优势为 \(+0.695 \pm 0.019\)，在 5 个种子上统计显著，\(p<0.01\)。
- **机制识别**：
  - H1 确认：GRU Memory 与 PRM 性能相当，差异 \(-0.023 \pm 0.028\) 不显著；
  - H2 拒绝：自预测没有额外收益；
  - H3 拒绝：随机辅助损失几乎无益
