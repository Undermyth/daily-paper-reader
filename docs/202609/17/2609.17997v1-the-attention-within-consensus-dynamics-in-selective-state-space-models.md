---
title: "The Attention Within: Consensus Dynamics in Selective State Space Models"
title_zh: 内在的注意力：选择性状态空间模型中的共识动力学
authors: "João Pedro Silvestre, Álvaro Rodríguez Abella, Paulo Tabuada"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.17997v1"
tags: ["query:la"]
score: 9.0
evidence: 选择性状态空间模型的递归可写成类似线性注意力的形式
tldr: 选择性状态空间模型（SSM）以递归混合 token，其作用类似 Transformer 的注意力，但递归是否也像注意力一样驱动 token 达成共识仍不清楚。本文从动力系统视角将跨层 token 演化建模为 ODE，并利用输入到状态稳定性分析。结果证明，在时变权重矩阵下共识平衡点具有局部指数稳定性，且可刻画其吸引域。预训练 Mamba-2 实验显示，输出门调节共识程度，阻止 token 完全坍缩，揭示 SSM 递归具有注意力式聚合作用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.17997v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 814, \"height\": 655, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.17997v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 818, \"height\": 624, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.17997v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 816, \"height\": 648, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.17997v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 923, \"height\": 634, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.17997v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 924, \"height\": 627, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.17997v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 925, \"height\": 1135, \"label\": \"Figure\"}]"
motivation: Transformer 中注意力会驱动 token 聚类至共识，而选择性 SSM 的递归虽类似线性注意力，其是否同样导致共识尚未被严格分析。
method: 将跨层 token 演化视为 ODE，借助输入到状态稳定性，分析时变权重矩阵下共识平衡点的局部指数稳定性与吸引域。
result: 证明 SSM 递归会聚合 token 并形成共识平衡点；在预训练 Mamba-2 上发现输出门调控共识强度，避免 token 完全坍缩。
conclusion: 表明 SSM 与 Transformer 的相似性更深层：递归也承担注意力式 token 聚合；输出门是控制共识程度的关键组件。
---

## 摘要
选择性状态空间模型（SSMs）近来已成为 Transformer 的一个引人注目的替代方案，兼具具有竞争力的性能与显著提升的推理效率。在每个 SSM 层中，一系列隐藏状态通过递推进行传播，从而混合不同 token 的信息。尽管使用了不同的机制，这种混合所起的作用与 Transformer 中的注意力类似。事实上，近期工作表明，这两种架构可能比最初看起来更为接近，因为这种递推可以写成类似线性注意力的形式。在 Transformer 中，已知注意力会驱使 token 聚集，即达成共识，并在极限下坍缩到单一方向。因此，我们提出：SSM 核心处的递推是否像 Transformer 中的注意力那样，驱使 token 达成共识？为回答这一问题，我们从动力系统的视角看待 SSM，将 token 跨层的演化建模为常微分方程。通过利用输入-状态稳定性论证，我们建立了共识平衡点的局部指数稳定性，并刻画了时变权重矩阵下的吸引域，而这一设定此前结果尚未涉及。由此我们表明，SSM 与 Transformer 之间的相似性确实更为深刻：SSM 核心处的递推会像注意力一样聚合 token。在预训练的 Mamba-2 模型上的数值实验表明，输出门是调节这种共识程度的组件，阻止 token 完全达成共识。

## Abstract
Selective state space models (SSMs) have recently emerged as a compelling alternative to transformers, combining competitive performance with substantially improved inference efficiency. At each SSM layer, a sequence of hidden states are propagated by a recurrence, mixing information of different tokens. Despite using a different mechanism, this mixing plays a role analogous to attention in transformers. In fact, recent works have shown that the two architectures may be closer than they first appear, as this recurrence admits a formulation akin to linear attention. In transformers, attention is known to drive the tokens to cluster, i.e., to reach consensus, collapsing in the limit to a single direction. Thus, we ask: does the recurrence at the core of SSMs drive the tokens to consensus, as attention does in transformers?   To answer this question, we take a dynamical systems perspective on SSMs, modeling the evolution of tokens across layers as an ordinary differential equation. By exploiting input-to-state stability arguments, we establish local exponential stability of the consensus equilibria and characterize their domain of attraction for time-varying weight matrices, a setting not addressed by previous results. We thereby show that the resemblance between SSMs and transformers does run deeper: the recurrence at the core of SSMs aggregates tokens just as attention does. Numerical experiments on a pretrained Mamba-2 model point to the output gate as the component that regulates the extent of this consensus, preventing the tokens from reaching it in full.

---

## 论文详细总结（自动生成）

# 论文总结：《The Attention Within: Consensus Dynamics in Selective State Space Models》

## 1. 核心问题与整体含义

- **研究动机**：Transformer 中的注意力机制已知会驱动 token 逐层聚集、最终坍缩到单一方向（即"共识"或 rank collapse）。选择性状态空间模型（SSM，如 Mamba-2）作为 Transformer 的高效替代方案，其核心递归机制可写成类似线性注意力的形式，但**这种结构相似性是否延伸到动力学行为**尚不清楚。
- **核心问题**：SSM 核心处的递归是否像注意力那样，驱使 token 跨层达成共识？
- **研究空白**：已有关于 SSM 共识现象的理论结果仅限于**时不变参数、无归一化**的狭窄设定，无法反映实际模型（权重逐层变化、使用 RMSNorm）。
- **整体含义**：本文从动力系统视角证明，SSM 的递归确实会像注意力一样聚合 token，揭示两种架构的相似性深入到动力学层面；同时指出输出门（output gate）是阻止 token 完全坍缩的关键组件。

## 2. 方法论

### 核心思想
将 Mamba-2 中 token 跨层的演化建模为球面上的**连续时间常微分方程（ODE）**，利用其**因果级联结构**（cascade/triangular structure）与**输入-状态稳定性（ISS）** 工具，分析共识平衡点的稳定性与吸引域。

### 关键技术细节

- **配置空间**：因 RMSNorm 将每个 token 投影到单位球面，状态空间为 $\left(S^{n-1}\right)^{\ell}$（$\ell$ 个 token，每 token 维度 $n$）。
- **Mamba-2 递推**：
  - 隐藏状态：$h_i^\mu(k) = A_i^\mu(k) h_{i-1}^\mu(k) + B_i^\mu(k) z_i^\mu(k)$
  - Token 更新：$z_i(k+1) = \pi\!\left(z_i(k) + \tau\, r_i(k)\right)$，其中 $\pi$ 为球面投影。
- **输入-输出闭式模型**（Lemma 2.1）：消去隐藏状态后得到
  $$z_i(k+1) = \pi\!\Big(z_i(k) + \tau \sum_{j=1}^{i} \beta_{ij}(k)\, z_j(k)\Big),\quad \beta_{ij}(k) = \big(z_i^\top S_{BC}(k) z_j\big) D_{ij}(k)$$
  其中 $S_{BC} = S_C^\top S_B$ 为交互核，$D_{ij}$ 为对角矩阵。
- **连续时间 ODE**（式 5）：
  $$\dot z_i = T_{z_i}\pi \cdot \sum_{j=1}^{i} \beta_{ij}(t)\, z_j,\quad 1\le i\le \ell$$
  其**级联结构**（第 $i$ 个 token 仅依赖前 $i$ 个 token）使 ISS 分析可递归进行。
- **假设条件**：
  - **Assumption 3.1**：参数有界；$D_{ij}$ 的主特征值 $\lambda^{\tilde\mu}_{ij}$ 唯一且谱隙 $\gamma>0$ 严格为正。
  - **Assumption 4.1（持续性激励 PE）**：交互项 $a_c$ 在任意长度 $T$ 窗口上平均为正。
  - **Assumption 5.1**：$S_{BC}(t)\succ \alpha I_n$（全局正定，用于刻画吸引域）。
- **主要理论结果**：
  - **Theorem 4.1**：在 PE 条件下，共识平衡点 $Z^*_\Sigma=(\sigma_1 e_{\tilde\mu},\dots,\sigma_\ell e_{\tilde\mu})$ **局部指数稳定**。
  - **Theorem 5.1**：在全局正定条件下，刻画了吸引域 $D_\ell(\tilde\mu)=(S^{n-1})^\ell \setminus B_\ell(\tilde\mu)$，并引入**各向异性度量 $c_\star$** 与**有效阈值 $c_{\rm eff}$**。
  - **Lemma 4.1**：SiLU 非线性使交互项的期望**严格为正**，而双线性版本期望为零——解释了 SiLU 促进聚类的机制。

## 3. 实验设计

- **模型**：预训练的 `mamba2-130m`（$n=768$，24 层，每头 64 通道），来自 HuggingFace。
- **场景**：由于理论结果是渐近的，通过**循环复用 24 层**将深度扩展到 200；取 $\ell=50$ 个 token，从词典均匀采样初始化；每组实验重复 **50 次**独立初始抽取，报告均值。
- **评价指标**：
  - $\bar\lambda_1$：Gram 矩阵归一化后的主方向能量占比（共线时为 1，正交时为 $1/\ell$）。
  - $r$：参与率（有效方向数，从 1 到 $\ell$）。
- **对比配置（4 组）**：
  1. 原始 Mamba-2 权重，**有/无输出门**。
  2. 单层重复应用（**时不变权重**），无门。
  3. 随机权重（每层独立重抽），**有/无 SiLU**。
  4. **持续性激励测试**：记录交互项 $q_i(t)$，寻找最小可接受窗口 $T^\star$（式 23）。
- **对比方法**：主要与"无门/无 SiLU/时不变"等消融变体对比，以及与 Transformer 的已有共识理论作定性类比。

## 4. 资源与算力

- **论文未明确说明**所使用的 GPU 型号、数量或训练/推理时长。
- 本研究属于**推理阶段的实验分析**（非模型训练），仅使用公开的预训练 checkpoint `mamba2-130m` 进行前向迭代，算力需求相对较小，但文中未给出具体硬件与耗时信息。

## 5. 实验数量与充分性

- **实验组数**：约 4 组主要配置，每组 50 次重复，共覆盖有门/无门、时不变/时变、有/无 SiLU、PE 验证等多个维度。
- **充分性评估**：
  - **优点**：消融设计清晰，逐项隔离了输出门、权重时变性、SiLU 三个因素，能有效支撑"输出门调控共识"的核心论点。
  - **不足**：
    - 仅使用**单一模型**（mamba2-130m）和单一规模，缺乏跨架构（如 Mamba-1、其他 SSM 变体）与跨规模的验证。
    - PE 实验**并未证明假设成立**，只是提供了"与假设一致"的支持性证据（沿采样轨迹，而非整个球冠）。
    - 实验为纯推理分析，未涉及下游任务性能，共识程度与任务表现的关系未被探讨。

## 6. 主要结论与发现

- **理论上**：在持续性激励下，SSM 的共识平衡点局部指数稳定；在全局正定条件下可刻画吸引域，**首次覆盖时变权重矩阵**这一实际设定。
- **实验上**：
  - 有门时 $\bar\lambda_1\approx0.45$、$r\approx4$；无门时 $\bar\lambda_1\approx0.8$、$r\approx1.4$——**输出门阻止 token 完全坍缩**。
  - 单层重复（时不变权重）时 $\bar\lambda_1>0.95$、$r\approx1.05$——**权重时变性削弱共识**，因为主特征值索引 $\tilde\mu$ 变化会中断收敛。
  - 随机权重下，有 SiLU 时 token 聚集（$\bar\lambda_1\to0.88$），无 SiLU 时反而发散（$\bar\lambda_1\to0.23$，$r>14$）——**SiLU 提供正偏置促进聚类**。
- **总体结论**：SSM 与 Transformer 的相似性深入到动力学层面，SSM 递归承担注意力式 token 聚合作用；**输出门是控制共识程度、防止 token 多样性完全丧失的关键组件**。

## 7. 优点

- **理论创新**：首次将 SSM 共识分析推广到时变权重矩阵，且纳入层归一化（球面约束），更贴近实际模型。
- **方法严谨**：借助 ISS 与级联结构，将高维耦合动力学分解为可递归分析的标量子系统，证明结构清晰。
- **理论与实验结合**：Lemma 4.1 从期望角度解释了 SiLU 的正偏置，与随机权重实验现象吻合。
- **可解释性强**：引入各向异性度量 $c_\star$ 与有效阈值 $c_{\rm eff}$，直观刻画吸引域大小与交互核各向异性的关系。
- **揭示新机制**：明确指出输出门这一常被忽略的组件在抑制共识中的作用，为架构设计提供启示。

## 8. 不足与局限

- **假设验证困难**：Assumption 4.1（PE）需要在整个球冠上对 inf 求积分，实践中难以直接验证；实验仅沿采样轨迹给出支持性证据，不能构成证明。
- **模型覆盖有限**：仅一个 130M 规模模型，未验证更大模型、其他 SSM 架构（如 Mamba-1、RWKV）或真实下游任务。
- **分析排除输出门**：理论模型刻意剔除输出门以隔离递归，但门恰恰是实际中限制共识的关键，理论与实际存在一定脱节（文中承认此点）。
- **吸引域的保守性**：有效阈值 $c_{\rm eff}$ 可能低估实际吸引域；各向异性情形下陷捕集可能具有正测度，甚至存在稳定平衡点。
- **算力信息缺失**：未报告硬件与耗时，复现成本不透明。
- **应用限制**：结论基于推理时循环扩展深度的非标准设置（实际模型深度固定为 24 层），其与真实部署行为的对应需进一步验证。

（完）
